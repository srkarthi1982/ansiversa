---
import SectionHeading from '../../../components/SectionHeading.astro';
import Button from '../../../components/Button.astro';
---
<section
  id="creative-title-workspace"
  class="border-t border-slate-200 bg-slate-50 py-16"
  x-data="() => ({ maker: $store.creativeTitleMaker })"
>
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <div class="flex flex-col gap-6 lg:flex-row lg:items-start lg:justify-between">
      <SectionHeading
        eyebrow="Workspace"
        title="Curate bundles, compare scores, and promote winning titles"
        description="Every variant is scored for clarity, novelty, SEO, CTR intent, and brand fit. Group them into A/B/C packs, filter by bucket, and push the best directly into downstream apps."
      />
      <div class="flex flex-wrap gap-3 text-xs text-slate-500">
        <span class="inline-flex items-center gap-2 rounded-full bg-white px-3 py-1 font-semibold text-slate-600 shadow-sm">
          <i class="fas fa-layer-group text-[10px] text-indigo-500"></i>
          Bundles auto-tagged by theme
        </span>
        <span class="inline-flex items-center gap-2 rounded-full bg-white px-3 py-1 font-semibold text-slate-600 shadow-sm">
          <i class="fas fa-shuffle text-[10px] text-indigo-500"></i>
          Ctrl/Cmd + . cycle bundles
        </span>
      </div>
    </div>

    <div class="mt-12 grid gap-10 lg:grid-cols-[1.05fr_0.95fr]">
      <div class="space-y-6">
        <article class="rounded-3xl border border-slate-200 bg-white p-6 shadow-soft">
          <header class="flex flex-wrap items-center justify-between gap-4">
            <div>
              <h2 class="text-xl font-semibold text-slate-900" x-text="maker.activeBundle?.label"></h2>
              <p class="text-sm text-slate-600" x-text="maker.activeBundle?.description"></p>
            </div>
            <div class="flex flex-wrap gap-2">
              <Button size="sm" class="rounded-full !px-4" @click.prevent="maker.regenerateBundle(maker.state.selectedBundleId)">
                <i class="fas fa-arrows-rotate text-xs"></i>
                Refresh pack
              </Button>
              <Button size="sm" variant="outline" class="rounded-full !px-4" @click.prevent="maker.cycleBundle('forward')">
                <i class="fas fa-forward text-xs"></i>
                Next bundle
              </Button>
            </div>
          </header>

          <div class="mt-6 flex flex-wrap gap-3">
            <template x-for="bundle in maker.state.bundles" :key="bundle.id">
              <button
                type="button"
                class="inline-flex items-center gap-2 rounded-full px-4 py-2 text-sm font-semibold transition"
                :class="maker.state.selectedBundleId === bundle.id
                  ? 'bg-indigo-600 text-white shadow shadow-indigo-500/30'
                  : 'bg-slate-100 text-slate-600 hover:-translate-y-0.5'"
                @click.prevent="maker.selectBundle(bundle.id)"
              >
                <i class="fas fa-folder-tree text-xs"></i>
                <span x-text="bundle.label"></span>
              </button>
            </template>
          </div>

          <div class="mt-8 space-y-4">
            <template x-for="variant in maker.variantsForBundle(maker.state.selectedBundleId)" :key="variant.id">
              <article
                class="rounded-2xl border border-slate-200 bg-slate-50/80 p-5 shadow-sm transition hover:-translate-y-0.5 hover:border-indigo-200 hover:bg-white"
                :class="maker.state.activeVariantId === variant.id ? 'border-indigo-300 bg-white shadow-md' : ''"
              >
                <div class="flex flex-wrap items-center justify-between gap-3">
                  <button
                    type="button"
                    class="text-left text-base font-semibold text-slate-900"
                    @click.prevent="maker.selectVariant(variant.id)"
                  >
                    <span x-text="variant.text"></span>
                  </button>
                  <span class="inline-flex items-center gap-2 rounded-full bg-white px-3 py-1 text-[11px] font-semibold text-slate-600">
                    <i class="fas fa-hashtag text-[10px] text-indigo-500"></i>
                    <span x-text="variant.bucket"></span>
                  </span>
                </div>
                <div class="mt-3 flex flex-wrap items-center gap-3 text-xs text-slate-500">
                  <span>
                    <i class="fas fa-chart-line text-indigo-500"></i>
                    <span class="font-semibold" x-text="`SEO ${Math.round(variant.scores.seo * 100)}`"></span>
                  </span>
                  <span>
                    <i class="fas fa-lightbulb text-amber-500"></i>
                    <span x-text="`Novelty ${Math.round(variant.scores.novelty * 100)}`"></span>
                  </span>
                  <span>
                    <i class="fas fa-bullhorn text-emerald-500"></i>
                    <span x-text="`CTR ${Math.round(variant.scores.ctrIntent * 100)}`"></span>
                  </span>
                  <span class="inline-flex items-center gap-1 rounded-full bg-slate-200/80 px-2.5 py-1 text-[11px] font-semibold text-slate-600">
                    <i class="fas fa-text-width text-[10px]"></i>
                    <span x-text="`${variant.chars} chars`"></span>
                  </span>
                  <template x-for="flag in variant.flags" :key="`${variant.id}-${flag}`">
                    <span class="inline-flex items-center gap-1 rounded-full bg-amber-100 px-2 py-1 text-[11px] font-semibold text-amber-700">
                      <i class="fas fa-triangle-exclamation text-[10px]"></i>
                      <span x-text="flag"></span>
                    </span>
                  </template>
                </div>
                <p class="mt-3 text-sm text-slate-600" x-text="variant.serpPreview"></p>
                <div class="mt-3 flex flex-wrap gap-2 text-[11px] font-semibold text-slate-500">
                  <template x-for="device in variant.devices" :key="`${variant.id}-${device}`">
                    <span class="inline-flex items-center gap-2 rounded-full bg-white px-3 py-1">
                      <i class="fas fa-sparkles text-[10px] text-indigo-500"></i>
                      <span x-text="device"></span>
                    </span>
                  </template>
                </div>
              </article>
            </template>
            <template x-if="maker.variantsForBundle(maker.state.selectedBundleId).length === 0">
              <p class="rounded-2xl border border-dashed border-slate-300 bg-white p-6 text-center text-sm text-slate-500">
                No variants generated yet for this bucket. Adjust your brief and click refresh to populate.
              </p>
            </template>
          </div>
        </article>
      </div>

      <aside class="space-y-6">
        <article class="rounded-3xl border border-white/60 bg-white p-6 shadow-soft">
          <h3 class="text-lg font-semibold text-slate-900">Theme buckets overview</h3>
          <p class="mt-1 text-sm text-slate-600">
            Every variant is tagged with a strategic bucket so you can diversify campaigns while staying on voice.
          </p>
          <div class="mt-4 grid gap-3 sm:grid-cols-2">
            <template x-for="bucket in maker.themeBuckets" :key="bucket.id">
              <div class="rounded-2xl border border-slate-200 bg-slate-50/80 p-3">
                <div class="flex items-center gap-2 text-sm font-semibold text-slate-900">
                  <i class="fas text-indigo-500 text-xs" :class="bucket.icon"></i>
                  <span x-text="bucket.label"></span>
                </div>
                <p class="mt-2 text-xs text-slate-500" x-text="bucket.description"></p>
              </div>
            </template>
          </div>
        </article>

        <article class="rounded-3xl border border-amber-100 bg-amber-50 p-6 shadow-sm">
          <h3 class="text-lg font-semibold text-amber-900">Risk & compliance flags</h3>
          <p class="mt-1 text-sm text-amber-800">
            Creative Title Maker highlights potential clickbait, keyword drift, or duplicates before you publish.
          </p>
          <ul class="mt-4 space-y-3 text-sm">
            <template x-for="flag in maker.riskFlags" :key="flag.id">
              <li class="rounded-2xl border border-amber-100 bg-white/70 p-4 shadow-sm">
                <div class="flex items-center justify-between text-amber-900">
                  <span class="font-semibold" x-text="flag.label"></span>
                  <span class="text-xs font-semibold uppercase tracking-wide" x-text="flag.severity"></span>
                </div>
                <p class="mt-2 text-xs text-amber-700" x-text="flag.recommendation"></p>
              </li>
            </template>
          </ul>
          <p class="mt-4 text-xs text-amber-700">
            <i class="fas fa-circle-info mr-1 text-[10px]"></i>
            Use <code class="rounded bg-white/70 px-1">/titles/api/check/seo</code> to run server-side compliance during automation.
          </p>
        </article>
      </aside>
    </div>
  </div>
</section>
