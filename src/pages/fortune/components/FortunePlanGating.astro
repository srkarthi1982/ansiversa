<section class="bg-slate-950 py-16 text-white" x-data="{ fortune: $store.fortuneOracle }">
  <div class="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">
    <div class="text-center">
      <h2 class="text-3xl font-bold tracking-tight sm:text-4xl">Plan gating & limits</h2>
      <p class="mt-3 text-base text-white/70">
        Free keeps the experience casual with light cooldowns, while Pro unlocks deeper spreads, archives, and unlimited shareables.
      </p>
    </div>
    <div class="mt-10 overflow-hidden rounded-3xl border border-white/10 bg-white/5 shadow-2xl shadow-indigo-900/40 backdrop-blur">
      <table class="min-w-full divide-y divide-white/10 text-sm">
        <thead class="bg-white/10 text-left text-xs uppercase tracking-[0.3em] text-white/70">
          <tr>
            <th scope="col" class="px-6 py-4 font-semibold">Feature</th>
            <th scope="col" class="px-6 py-4 font-semibold">Free</th>
            <th scope="col" class="px-6 py-4 font-semibold">Pro</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-white/5">
          <template x-for="feature in fortune.state.planFeatures" :key="feature.label">
            <tr class="transition hover:bg-white/10">
              <th scope="row" class="px-6 py-4 text-left text-sm font-semibold text-white" x-text="feature.label"></th>
              <td class="px-6 py-4 text-sm text-white/80">
                <template x-if="feature.free === true">
                  <span class="inline-flex items-center gap-2 text-emerald-300">
                    <i class="fas fa-check text-xs"></i>
                    Included
                  </span>
                </template>
                <template x-if="feature.free === false">
                  <span class="inline-flex items-center gap-2 text-rose-300">
                    <i class="fas fa-ban text-xs"></i>
                    Locked
                  </span>
                </template>
                <template x-if="typeof feature.free === 'string'">
                  <span x-text="feature.free"></span>
                </template>
              </td>
              <td class="px-6 py-4 text-sm text-white/80">
                <template x-if="feature.pro === true">
                  <span class="inline-flex items-center gap-2 text-emerald-300">
                    <i class="fas fa-check text-xs"></i>
                    Included
                  </span>
                </template>
                <template x-if="feature.pro === false">
                  <span class="inline-flex items-center gap-2 text-rose-300">
                    <i class="fas fa-ban text-xs"></i>
                    Locked
                  </span>
                </template>
                <template x-if="typeof feature.pro === 'string'">
                  <span x-text="feature.pro"></span>
                </template>
              </td>
            </tr>
          </template>
        </tbody>
      </table>
    </div>
    <div class="mt-6 grid gap-4 text-xs text-white/70 md:grid-cols-3">
      <div class="rounded-2xl border border-white/10 bg-white/5 px-4 py-3">
        <p class="font-semibold text-white/90">Yes/No rate limits</p>
        <p class="mt-1">Free: 10s cooldown, 3 per minute. Pro: 3s cooldown, 10 per minute.</p>
      </div>
      <div class="rounded-2xl border border-white/10 bg-white/5 px-4 py-3">
        <p class="font-semibold text-white/90">Rerolls</p>
        <p class="mt-1">Free gets one daily reroll, Pro unlocks three with reason logging.</p>
      </div>
      <div class="rounded-2xl border border-white/10 bg-white/5 px-4 py-3">
        <p class="font-semibold text-white/90">Archives & reminders</p>
        <p class="mt-1">Zodiac archive, weekly recaps, and scheduled reminders stay Pro-only to manage infrastructure load.</p>
      </div>
    </div>
  </div>
</section>
