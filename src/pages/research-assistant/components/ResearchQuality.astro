---
import SectionHeading from "../../../components/SectionHeading.astro";
import { accessibilityFeatures, validationRules } from "../data/researchData";
---
<section class="bg-slate-900 py-16 text-white sm:py-20" aria-labelledby="research-quality-title">
  <div class="mx-auto max-w-6xl space-y-10 px-4 sm:px-6 lg:px-8">
    <SectionHeading
      eyebrow="Quality guardrails"
      title="Validation and accessibility baked into the flow"
      description="Input constraints and inclusive interactions keep research capture clean while remaining usable for every learner."
      id="research-quality-title"
      class="text-white"
    />
    <div class="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]">
      <article class="rounded-3xl border border-white/10 bg-white/5 p-6 shadow-lg backdrop-blur">
        <h3 class="text-sm font-semibold uppercase tracking-wide text-indigo-200">Validation rules</h3>
        <ul class="mt-4 space-y-3 text-sm text-slate-100">
          {validationRules.map((rule) => (
            <li class="flex items-start gap-3 rounded-2xl border border-white/10 bg-white/5 px-4 py-3" key={rule.label}>
              <span class="mt-0.5 rounded-full bg-indigo-500/20 p-2 text-indigo-200">
                <i class="fas fa-shield-halved text-[11px]"></i>
              </span>
              <div>
                <p class="text-sm font-semibold text-white">{rule.label}</p>
                <p class="text-xs text-slate-300">{rule.description}</p>
              </div>
            </li>
          ))}
        </ul>
      </article>
      <article class="flex flex-col gap-4 rounded-3xl border border-white/10 bg-white/5 p-6 shadow-lg backdrop-blur">
        <h3 class="text-sm font-semibold uppercase tracking-wide text-indigo-200">Accessibility</h3>
        <p class="text-sm text-slate-100">
          Keyboard shortcuts, contrast, and localization-ready typography ensure the research workspace performs for every
          background.
        </p>
        <ul class="space-y-3 text-sm text-slate-100">
          {accessibilityFeatures.map((feature) => (
            <li class="flex items-start gap-3 rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3" key={feature}>
              <i class="fas fa-universal-access mt-0.5 text-[12px] text-indigo-200"></i>
              <span>{feature}</span>
            </li>
          ))}
        </ul>
      </article>
    </div>
  </div>
</section>
