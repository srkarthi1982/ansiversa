---
import SectionHeading from "../../../components/SectionHeading.astro";

const variableRows = [
  {
    symbol: "s",
    name: "Displacement",
    unit: "m",
    dimension: "L",
    notes: "Positive along motion axis"
  },
  {
    symbol: "u",
    name: "Initial velocity",
    unit: "m/s",
    dimension: "L T⁻¹",
    notes: "Known from start conditions"
  },
  {
    symbol: "a",
    name: "Acceleration",
    unit: "m/s²",
    dimension: "L T⁻²",
    notes: "Constant within interval"
  },
  {
    symbol: "t",
    name: "Time interval",
    unit: "s",
    dimension: "T",
    notes: "> 0"
  },
];

const assumptions = [
  "Motion is colinear with defined axis.",
  "Acceleration remains constant across Δt.",
  "Air resistance and external forces are negligible.",
];

const identityForms = [
  "t = \\frac{-u + \\sqrt{u^2 + 2as}}{a}",
  "u = \\sqrt{u^2 + 2as} - at",
  "s = \\tfrac{(u + v)}{2} t",
];

const exampleSteps = [
  "Substitute: s = 5 m/s * 3 s + 0.5 * 2 m/s² * (3 s)²",
  "Compute: s = 15 m + 0.5 * 2 * 9",
  "Result: s = 15 m + 9 m = 24 m",
];
---
<section class="bg-white py-20 sm:py-24">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <SectionHeading
      eyebrow="Formula detail view"
      title="Rich cards that explain every symbol"
      description="Each formula page renders KaTeX perfectly, spells out symbols and dimensions, and offers identities, assumptions, and examples side-by-side."
      align="center"
      containerClass="max-w-3xl"
    />
    <div class="mt-16 grid gap-10 lg:grid-cols-[1.1fr_0.9fr] lg:items-start">
      <article class="space-y-6 rounded-3xl border border-slate-200 bg-slate-50/80 p-8 shadow-sm">
        <header class="flex flex-wrap items-start justify-between gap-4">
          <div class="space-y-2">
            <p class="text-xs font-semibold uppercase tracking-[0.35em] text-indigo-500">SUVAT displacement</p>
            <p class="font-mono text-2xl text-slate-900">s = ut + ½ a t²</p>
            <p class="text-sm text-slate-600">
              Predict displacement when acceleration is constant. Equivalent to integrating velocity over the interval.
            </p>
          </div>
          <span class="inline-flex items-center gap-2 rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1 text-xs font-semibold uppercase tracking-[0.3em] text-emerald-600">
            Dimension L
          </span>
        </header>
        <div class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm">
          <h3 class="text-sm font-semibold uppercase tracking-[0.3em] text-slate-500">Variables</h3>
          <table class="mt-4 w-full text-left text-sm text-slate-600">
            <thead class="text-[11px] uppercase tracking-[0.35em] text-indigo-400">
              <tr>
                <th class="pb-2">Symbol</th>
                <th class="pb-2">Meaning</th>
                <th class="pb-2">Unit</th>
                <th class="pb-2">Dimension</th>
                <th class="pb-2">Notes</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-slate-200 text-sm">
              {variableRows.map((row) => (
                <tr class="hover:bg-indigo-50/60">
                  <td class="py-3 font-semibold text-slate-900">{row.symbol}</td>
                  <td class="py-3">{row.name}</td>
                  <td class="py-3 font-mono text-slate-500">{row.unit}</td>
                  <td class="py-3 font-mono text-slate-500">{row.dimension}</td>
                  <td class="py-3 text-xs text-slate-500">{row.notes}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
        <div class="grid gap-4 sm:grid-cols-2">
          <div class="space-y-3 rounded-3xl border border-slate-200 bg-white/90 p-5">
            <h3 class="text-sm font-semibold uppercase tracking-[0.3em] text-slate-500">Assumptions</h3>
            <ul class="space-y-2 text-sm text-slate-600">
              {assumptions.map((item) => (
                <li class="flex items-start gap-3">
                  <span class="mt-1 inline-flex h-5 w-5 items-center justify-center rounded-full bg-indigo-100 text-indigo-500">
                    <i class="fas fa-check text-[10px]" aria-hidden="true"></i>
                  </span>
                  <span>{item}</span>
                </li>
              ))}
            </ul>
          </div>
          <div class="space-y-3 rounded-3xl border border-slate-200 bg-white/90 p-5">
            <h3 class="text-sm font-semibold uppercase tracking-[0.3em] text-slate-500">Identity forms</h3>
            <ul class="space-y-2 text-sm text-slate-600">
              {identityForms.map((identity) => (
                <li class="flex items-center justify-between gap-3 rounded-2xl border border-indigo-100 bg-indigo-50/70 px-3 py-2">
                  <span class="font-mono text-sm text-indigo-700">{identity}</span>
                  <button
                    type="button"
                    class="inline-flex h-8 w-8 items-center justify-center rounded-full border border-indigo-200 bg-white text-indigo-500 hover:bg-indigo-500 hover:text-white"
                    aria-label={`Copy identity ${identity}`}
                  >
                    <i class="fas fa-copy text-xs" aria-hidden="true"></i>
                  </button>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </article>
      <aside class="space-y-6 rounded-3xl border border-indigo-100 bg-gradient-to-br from-indigo-50 via-white to-slate-50 p-6 shadow-inner">
        <div class="space-y-3 rounded-3xl border border-slate-200 bg-white/90 p-5">
          <h3 class="text-sm font-semibold uppercase tracking-[0.3em] text-indigo-500">Dimensional check</h3>
          <p class="text-sm text-slate-600">
            LHS dimension L = RHS dimension L (ut → L, ½at² → L). Any mismatch is flagged before publishing.
          </p>
          <div class="rounded-2xl border border-indigo-100 bg-indigo-50/70 p-3 text-xs text-indigo-600">
            <p><strong>Matrix:</strong> L¹ M⁰ T⁰ I⁰ Θ⁰ N⁰ J⁰</p>
            <p class="mt-1">All contributing terms resolve to L¹.</p>
          </div>
        </div>
        <div class="space-y-3 rounded-3xl border border-slate-200 bg-white/90 p-5">
          <h3 class="text-sm font-semibold uppercase tracking-[0.3em] text-indigo-500">Worked example</h3>
          <p class="text-sm text-slate-600">
            Runner starts at 5 m/s and accelerates at 2 m/s² for 3 seconds. What distance is covered?
          </p>
          <ol class="space-y-2 text-sm text-slate-600">
            {exampleSteps.map((step, index) => (
              <li class="flex gap-3 rounded-2xl border border-slate-200 bg-slate-50/80 px-3 py-2">
                <span class="mt-0.5 inline-flex h-6 w-6 flex-none items-center justify-center rounded-full bg-indigo-100 font-semibold text-indigo-600">
                  {index + 1}
                </span>
                <span>{step}</span>
              </li>
            ))}
          </ol>
          <p class="mt-3 inline-flex items-center gap-2 rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1 text-xs font-semibold uppercase tracking-[0.3em] text-emerald-600">
            Answer 24 m • Passes unit check
          </p>
        </div>
        <div class="rounded-3xl border border-slate-200 bg-white/90 p-5">
          <h3 class="text-sm font-semibold uppercase tracking-[0.3em] text-indigo-500">Export controls</h3>
          <p class="mt-2 text-sm text-slate-600">
            Copy LaTeX, Markdown, or export polished PDF packets. Auto-include symbol glossary and assumptions.
          </p>
          <div class="mt-4 flex flex-wrap gap-3">
            <button type="button" class="inline-flex items-center gap-2 rounded-xl border border-indigo-100 bg-indigo-50 px-3 py-2 text-xs font-semibold text-indigo-600">
              <i class="fas fa-code text-[11px]" aria-hidden="true"></i>
              LaTeX
            </button>
            <button type="button" class="inline-flex items-center gap-2 rounded-xl border border-indigo-100 bg-indigo-50 px-3 py-2 text-xs font-semibold text-indigo-600">
              <i class="fas fa-file-lines text-[11px]" aria-hidden="true"></i>
              Markdown
            </button>
            <button type="button" class="inline-flex items-center gap-2 rounded-xl border border-indigo-100 bg-indigo-50 px-3 py-2 text-xs font-semibold text-indigo-600">
              <i class="fas fa-file-pdf text-[11px]" aria-hidden="true"></i>
              PDF
            </button>
          </div>
        </div>
      </aside>
    </div>
  </div>
</section>
