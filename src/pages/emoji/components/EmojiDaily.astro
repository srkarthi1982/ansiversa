<section class="bg-slate-50 py-16 sm:py-20">
  <div class="mx-auto max-w-6xl space-y-12 px-4 sm:px-6 lg:px-8">
    <header class="space-y-3 text-center lg:text-left">
      <span class="inline-flex items-center gap-2 rounded-full bg-amber-100 px-4 py-1.5 text-sm font-semibold text-amber-700">
        <i class="fas fa-calendar-star text-xs"></i>
        Daily emoji challenge
      </span>
      <h2 class="text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">One puzzle per day, shared leaderboard</h2>
      <p class="text-base text-slate-600">
        A timezone-aware seed keeps everyone solving the same puzzle within a 24 hour window. Scores, times, and streaks are
        verified before they hit the board.
      </p>
    </header>
    <div class="grid gap-6 lg:grid-cols-[2fr_3fr]">
      <div class="space-y-5 rounded-3xl border border-amber-200 bg-white p-6 shadow-sm">
        <div class="flex items-start justify-between gap-3">
          <div>
            <p class="text-xs font-semibold uppercase tracking-wide text-amber-600">Today's puzzle</p>
            <p class="text-sm text-slate-500" x-text="emoji.state.daily?.date"></p>
          </div>
          <span class="inline-flex items-center gap-2 rounded-full bg-amber-100 px-3 py-1 text-xs font-semibold text-amber-700">
            <i class="fas fa-sparkle text-[10px]"></i>
            <span x-text="emoji.state.daily?.difficulty"></span>
          </span>
        </div>
        <div class="rounded-2xl border border-amber-100 bg-amber-50/70 p-4 text-center text-4xl">
          <span x-text="emoji.state.daily?.emojis ?? '🌍❓'"></span>
        </div>
        <p class="rounded-2xl border border-amber-100 bg-white p-4 text-sm text-slate-600" x-text="emoji.state.daily?.clue"></p>
        <button
          type="button"
          class="inline-flex items-center gap-2 rounded-full bg-amber-500 px-4 py-2 text-sm font-semibold text-white shadow-sm transition hover:bg-amber-600"
          @click.prevent="emoji.refreshDailySummary()"
        >
          <i class="fas fa-arrow-rotate-right text-xs"></i>
          Refresh leaderboard snapshot
        </button>
        <p class="text-xs text-slate-500">
          <i class="fas fa-shield-check mr-2 text-[10px] text-amber-500"></i>
          <span>Server validates submissions, checks hint usage, and locks the board at midnight local time.</span>
        </p>
      </div>
      <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
        <header class="flex items-center justify-between">
          <div>
            <p class="text-sm font-semibold text-slate-900">Leaderboard snapshot</p>
            <p class="text-xs text-slate-500">
              <span x-text="emoji.state.daily?.solved.toLocaleString() ?? 0"></span>
              solvers · median
              <span x-text="emoji.state.daily ? `${emoji.state.daily.medianTimeSeconds}s` : '—'"></span>
            </p>
          </div>
          <span class="inline-flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">
            <i class="fas fa-medal text-[10px]"></i>
            Top 3
          </span>
        </header>
        <ol class="mt-6 space-y-3">
          <template x-for="entry in emoji.state.daily?.leaderboard ?? []" :key="entry.rank">
            <li
              class="flex items-center gap-3 rounded-2xl border border-slate-100 bg-slate-50/60 px-4 py-3"
              :class="entry.rank === 1 ? 'border-amber-200 bg-amber-50/80' : ''"
            >
              <span
                class="flex h-10 w-10 items-center justify-center rounded-full text-sm font-semibold"
                :class="entry.rank === 1 ? 'bg-amber-500 text-white' : 'bg-slate-200 text-slate-600'"
                x-text="entry.rank"
              ></span>
              <div class="flex-1">
                <p class="font-semibold text-slate-900" x-text="entry.handle"></p>
                <p class="text-xs text-slate-500">
                  <span x-text="`${entry.score} pts`"></span>
                  ·
                  <span x-text="entry.time"></span>
                  · streak
                  <span x-text="entry.streak"></span>
                </p>
              </div>
              <span class="inline-flex items-center gap-2 rounded-full bg-white px-3 py-1 text-xs font-semibold text-slate-500">
                <i class="fas fa-fire text-[10px] text-amber-500"></i>
                <span x-text="entry.streak"></span>
              </span>
            </li>
          </template>
        </ol>
      </div>
    </div>
  </div>
</section>
