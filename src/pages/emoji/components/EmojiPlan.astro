<section class="bg-slate-900 py-16 sm:py-20">
  <div class="mx-auto max-w-6xl space-y-12 px-4 sm:px-6 lg:px-8 text-white">
    <header class="space-y-3 text-center lg:text-left">
      <span class="inline-flex items-center gap-2 rounded-full bg-white/10 px-4 py-1.5 text-sm font-semibold">
        <i class="fas fa-lock-open text-xs text-emerald-300"></i>
        Plan gating
      </span>
      <h2 class="text-3xl font-bold tracking-tight sm:text-4xl">Free to start, Pro unlocks competitive tools</h2>
      <p class="text-base text-slate-300">
        Starter players enjoy Classic and Daily modes with base hints, while Pro members get Versus, Time Attack, imports,
        advanced analytics, and unlimited invites.
      </p>
    </header>
    <div class="grid gap-6 lg:grid-cols-2">
      <template x-for="tier in emoji.state.planTiers" :key="tier.id">
        <article
          class="flex h-full flex-col rounded-3xl border border-white/10 bg-white/5 p-6"
          :class="tier.id === 'pro' ? 'ring-2 ring-emerald-300' : ''"
        >
          <div class="flex items-start justify-between">
            <div>
              <p class="text-xs font-semibold uppercase tracking-wide text-emerald-300" x-text="tier.name"></p>
              <p class="mt-1 text-3xl font-bold" x-text="tier.price"></p>
            </div>
            <span class="inline-flex items-center gap-2 rounded-full bg-emerald-400/20 px-3 py-1 text-xs font-semibold text-emerald-200">
              <i class="fas fa-star text-[10px]"></i>
              <span x-text="tier.inviteLimit"></span>
            </span>
          </div>
          <p class="mt-3 text-sm text-slate-200" x-text="tier.highlight"></p>
          <ul class="mt-4 space-y-2 text-sm text-slate-100">
            <template x-for="feature in tier.includes" :key="feature">
              <li class="flex items-start gap-2">
                <i class="fas fa-check-circle text-[10px] text-emerald-300"></i>
                <span x-text="feature"></span>
              </li>
            </template>
          </ul>
          <button
            type="button"
            class="mt-6 inline-flex items-center gap-2 self-start rounded-full bg-emerald-400 px-4 py-2 text-xs font-semibold uppercase tracking-wide text-slate-900 shadow-sm hover:bg-emerald-300"
            @click.prevent="emoji.setActiveMode(tier.id === 'pro' ? 'versus' : 'classic')"
          >
            <i class="fas fa-unlock text-[10px]"></i>
            Explore tier
          </button>
        </article>
      </template>
    </div>
    <div class="rounded-3xl border border-white/10 bg-white/5 p-6 text-xs text-slate-200">
      <p>
        <i class="fas fa-circle-info mr-2 text-[10px] text-emerald-300"></i>
        Pro subscriptions bundle Supabase-powered leaderboards, anti-replay audits, and private pack publishing controls for
        classrooms or creator collectives.
      </p>
    </div>
  </div>
</section>
