<section class="bg-white py-16 sm:py-20">
  <div class="mx-auto max-w-6xl space-y-12 px-4 sm:px-6 lg:px-8">
    <header class="space-y-4 text-center lg:text-left">
      <span class="inline-flex items-center gap-2 rounded-full bg-purple-100 px-4 py-1.5 text-sm font-semibold text-purple-700">
        <i class="fas fa-boxes-packing text-xs"></i>
        Packs & imports
      </span>
      <h2 class="text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">Curate, import, and moderate emoji packs</h2>
      <p class="text-base text-slate-600">
        Built-in sets cover family-safe classics while Pro creators can import CSV/JSON bundles, translate aliases, and publish
        moderated community drops.
      </p>
    </header>
    <div class="grid gap-6 lg:grid-cols-3">
      <template x-for="pack in emoji.state.packs" :key="pack.id">
        <article
          class="flex h-full flex-col rounded-3xl border p-6 shadow-sm transition"
          :class="emoji.state.activePackId === pack.id ? 'border-purple-300 bg-white shadow-lg' : 'border-slate-200 bg-white/80 hover:border-purple-200'"
        >
          <div class="flex items-start justify-between gap-3">
            <div>
              <p class="text-xs font-semibold uppercase tracking-wide text-purple-600" x-text="pack.visibility"></p>
              <h3 class="mt-2 text-xl font-semibold text-slate-900" x-text="pack.title"></h3>
              <p class="text-sm text-slate-500">
                <span class="font-semibold" x-text="pack.size"></span>
                puzzles · <span x-text="pack.language.toUpperCase()"></span>
              </p>
            </div>
            <button
              type="button"
              class="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-3 py-1 text-xs font-semibold text-purple-600 shadow-sm"
              @click.prevent="emoji.setActivePack(pack.id)"
            >
              <i class="fas fa-star text-[10px]"></i>
              Feature
            </button>
          </div>
          <div class="mt-4 flex flex-wrap gap-2 text-xs text-slate-500">
            <template x-for="mix in pack.difficultyMix" :key="mix">
              <span class="inline-flex items-center gap-2 rounded-full bg-purple-50 px-3 py-1 font-semibold text-purple-700">
                <i class="fas fa-chart-pie text-[10px]"></i>
                <span x-text="mix"></span>
              </span>
            </template>
          </div>
          <ul class="mt-4 space-y-2 text-sm text-slate-600">
            <template x-for="feature in pack.features" :key="feature">
              <li class="flex items-start gap-2">
                <i class="fas fa-check text-[10px] text-purple-500"></i>
                <span x-text="feature"></span>
              </li>
            </template>
          </ul>
          <div class="mt-4 flex flex-wrap gap-2 text-2xl">
            <template x-for="emojiIcon in pack.featuredEmojis" :key="emojiIcon">
              <span class="inline-flex h-11 w-11 items-center justify-center rounded-2xl bg-purple-50 text-xl">
                <span x-text="emojiIcon"></span>
              </span>
            </template>
          </div>
        </article>
      </template>
    </div>
    <div class="grid gap-6 lg:grid-cols-2">
      <div class="rounded-3xl border border-slate-200 bg-white p-6">
        <h3 class="text-lg font-semibold text-slate-900">Import pipeline</h3>
        <ul class="mt-4 space-y-3 text-sm text-slate-600">
          <li class="flex items-start gap-3">
            <i class="fas fa-file-import mt-1 text-[10px] text-purple-500"></i>
            <div>
              <p class="font-semibold text-slate-800">CSV/JSON validation</p>
              <p>Hash-based dedupe prevents duplicate puzzles, while required fields enforce emojis and explanations.</p>
            </div>
          </li>
          <li class="flex items-start gap-3">
            <i class="fas fa-language mt-1 text-[10px] text-purple-500"></i>
            <div>
              <p class="font-semibold text-slate-800">Localization-ready aliases</p>
              <p>Normalize accents and provide per-language accepted answers for bilingual classrooms.</p>
            </div>
          </li>
          <li class="flex items-start gap-3">
            <i class="fas fa-user-shield mt-1 text-[10px] text-purple-500"></i>
            <div>
              <p class="font-semibold text-slate-800">Moderation workflow</p>
              <p>Community packs queue for review with profanity filters, audit logs, and publish scheduling.</p>
            </div>
          </li>
        </ul>
      </div>
      <div class="rounded-3xl border border-purple-200 bg-purple-50 p-6">
        <h3 class="text-lg font-semibold text-purple-900">Creator toolbox</h3>
        <ul class="mt-4 space-y-3 text-sm text-purple-900/80">
          <template x-for="tool in emoji.state.creatorTools" :key="tool.id">
            <li class="rounded-2xl border border-purple-200 bg-white/70 p-4">
              <p class="font-semibold text-purple-900" x-text="tool.name"></p>
              <p class="text-sm" x-text="tool.description"></p>
              <ul class="mt-3 space-y-1 text-xs text-purple-800">
                <template x-for="feature in tool.features" :key="feature">
                  <li class="flex items-start gap-2">
                    <i class="fas fa-sparkles text-[10px]"></i>
                    <span x-text="feature"></span>
                  </li>
                </template>
              </ul>
            </li>
          </template>
        </ul>
      </div>
    </div>
  </div>
</section>
