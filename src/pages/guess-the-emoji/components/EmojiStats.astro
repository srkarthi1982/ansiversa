<section class="bg-white py-16 sm:py-20">
  <div class="mx-auto max-w-6xl space-y-12 px-4 sm:px-6 lg:px-8">
    <header class="space-y-3 text-center lg:text-left">
      <span class="inline-flex items-center gap-2 rounded-full bg-indigo-100 px-4 py-1.5 text-sm font-semibold text-indigo-700">
        <i class="fas fa-chart-line text-xs"></i>
        Stats & versus
      </span>
      <h2 class="text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">Track streaks, accuracy, and friendly rivalries</h2>
      <p class="text-base text-slate-600">
        Personal analytics highlight streak momentum, language preferences, and speed trends. Async Versus lets friends compare
        runs with tamper-proof validation.
      </p>
    </header>
    <div class="grid gap-6 lg:grid-cols-[3fr_2fr]">
      <div class="rounded-3xl border border-indigo-200 bg-white p-6 shadow-sm">
        <div class="grid gap-4 sm:grid-cols-2">
          <div class="rounded-2xl border border-indigo-100 bg-indigo-50/70 p-4">
            <p class="text-xs font-semibold uppercase tracking-wide text-indigo-600">Daily streak</p>
            <p class="mt-2 text-3xl font-bold text-slate-900">
              <span x-text="emoji.state.stats?.streakDays ?? 0"></span>
              <span class="text-sm font-semibold text-slate-500">of</span>
              <span class="text-sm font-semibold text-slate-500" x-text="emoji.state.stats?.bestStreak ?? 0"></span>
            </p>
          </div>
          <div class="rounded-2xl border border-indigo-100 bg-white p-4">
            <p class="text-xs font-semibold uppercase tracking-wide text-indigo-600">Accuracy</p>
            <p class="mt-2 text-3xl font-bold text-slate-900">
              <span x-text="Math.round((emoji.state.stats?.accuracy ?? 0) * 100)"></span>%
            </p>
            <p class="text-xs text-slate-500">Average solve time
              <span class="font-semibold text-slate-700" x-text="`${emoji.state.stats?.averageSolveSeconds ?? 0}s`"></span>
            </p>
          </div>
          <div class="rounded-2xl border border-slate-200 bg-white p-4">
            <p class="text-xs font-semibold uppercase tracking-wide text-slate-500">Solved puzzles</p>
            <p class="mt-2 text-3xl font-bold text-slate-900" x-text="emoji.state.stats?.puzzlesSolved.toLocaleString() ?? '0'"></p>
            <p class="text-xs text-slate-500">Hints used: <span x-text="emoji.state.stats?.hintsUsed"></span></p>
          </div>
          <div class="rounded-2xl border border-slate-200 bg-white p-4">
            <p class="text-xs font-semibold uppercase tracking-wide text-slate-500">Preferences</p>
            <p class="mt-2 text-sm text-slate-600">
              <span class="font-semibold text-slate-800">Language:</span>
              <span x-text="emoji.state.stats?.preferredLanguage"></span>
            </p>
            <p class="text-sm text-slate-600">
              <span class="font-semibold text-slate-800">Top category:</span>
              <span x-text="emoji.state.stats?.topCategory"></span>
            </p>
          </div>
        </div>
        <div class="mt-6 rounded-2xl border border-indigo-100 bg-white p-4 text-xs text-slate-500">
          <p>
            <i class="fas fa-info-circle mr-2 text-[10px] text-indigo-500"></i>
            Export stats to CSV or sync with classroom dashboards. Time Attack analytics include percentile charts and accuracy
            drift over time.
          </p>
        </div>
      </div>
      <div class="space-y-6">
        <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
          <header class="flex items-start justify-between">
            <div>
              <p class="text-sm font-semibold text-slate-900">Async Versus highlight</p>
              <p class="text-xs text-slate-500" x-text="emoji.state.versus?.mode"></p>
            </div>
            <span class="inline-flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">
              <i class="fas fa-swords text-[10px]"></i>
              <span x-text="emoji.state.versus?.status"></span>
            </span>
          </header>
          <div class="mt-4 space-y-2 text-sm text-slate-600">
            <p>
              <span class="font-semibold text-slate-900" x-text="emoji.state.versus?.challenger"></span>
              <span class="text-xs text-slate-500">vs</span>
              <span class="font-semibold text-slate-900" x-text="emoji.state.versus?.opponent"></span>
            </p>
            <p>
              <span class="inline-flex items-center gap-2 rounded-full bg-indigo-50 px-3 py-1 text-xs font-semibold text-indigo-700">
                <i class="fas fa-trophy text-[10px]"></i>
                <span x-text="`${emoji.state.versus?.challengerScore ?? 0} : ${emoji.state.versus?.opponentScore ?? 0}`"></span>
              </span>
            </p>
            <p class="text-sm text-slate-600" x-text="emoji.state.versus?.summary"></p>
          </div>
          <ol class="mt-4 space-y-2 text-xs text-slate-500">
            <template x-for="step in emoji.state.versus?.steps ?? []" :key="step.label">
              <li class="flex items-start gap-2">
                <i class="fas fa-seedling text-[10px] text-indigo-500"></i>
                <div>
                  <p class="font-semibold text-slate-700" x-text="step.label"></p>
                  <p x-text="step.detail"></p>
                </div>
              </li>
            </template>
          </ol>
          <button
            type="button"
            class="mt-4 inline-flex items-center gap-2 rounded-full bg-indigo-500 px-4 py-2 text-xs font-semibold uppercase tracking-wide text-white shadow-sm hover:bg-indigo-600"
            @click.prevent="emoji.generateVersusInvite()"
          >
            <i class="fas fa-link text-[10px]"></i>
            Copy invite URL
          </button>
        </div>
        <div class="rounded-3xl border border-slate-200 bg-slate-50 p-6 text-xs text-slate-500">
          <p>
            <i class="fas fa-flag-checkered mr-2 text-[10px] text-indigo-500"></i>
            Versus invites expire after validation and include replay hashes so players can review timings puzzle by puzzle.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
