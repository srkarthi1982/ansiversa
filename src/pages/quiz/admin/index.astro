---
import Layout from "../../../layouts/Layout.astro";
---
<Layout>
  <main class="bg-slate-50 min-h-screen">
    <section class="py-16" x-data="() => ({ loader: $store.loader, admin: $store.admin ?? {} })">
      <div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 space-y-6">
        <header class="space-y-3 text-center">
          <h1 class="text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">Quiz Admin</h1>
          <p class="text-sm text-slate-600 sm:text-base">
            Manage every quiz platform from a single control centre. Create new experiences, keep descriptions fresh, and monitor activity in one place.
          </p>
        </header>
        <div class="flex flex-wrap items-center justify-center gap-3">
          <a
            href="/quiz/admin/platform"
            class="inline-flex items-center gap-2 rounded-full bg-indigo-600 px-5 py-2 text-sm font-semibold text-white shadow-sm transition hover:bg-indigo-700"
          >
            <i class="fas fa-layer-group text-sm"></i>
            Open platform manager
          </a>
          <a
            href="/quiz"
            class="inline-flex items-center gap-2 rounded-full border border-slate-200 px-5 py-2 text-sm font-semibold text-slate-600 transition hover:border-slate-300 hover:text-slate-800"
          >
            View public page
          </a>
        </div>
      </div>
    </section>
  </main>
</Layout>
