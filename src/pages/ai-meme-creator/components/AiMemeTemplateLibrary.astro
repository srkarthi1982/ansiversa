---
import SectionHeading from '../../../components/SectionHeading.astro';
---
<section
  id="ai-meme-library"
  class="border-t border-slate-200 bg-white py-16"
  x-data="() => ({ meme: $store.aiMemeCreator })"
>
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <SectionHeading
      eyebrow="Template library"
      title="Search look-alike meme formats tuned for campaigns"
      description="Pick from trending reactions, corporate-safe explainers, or multi-panel comics. Filter by aspect ratio, panel count, and animation without leaving the flow."
    />

    <div class="mt-12 grid gap-10 lg:grid-cols-[0.85fr_1.15fr]">
      <aside class="space-y-6">
        <div class="rounded-3xl border border-slate-200 bg-slate-50 p-6 shadow-inner">
          <h3 class="text-sm font-semibold text-slate-800">Categories</h3>
          <p class="mt-2 text-xs text-slate-500">
            Swap categories to load curated templates. Tags follow trending topics pulled from campaign analytics.
          </p>
          <div class="mt-4 flex flex-wrap gap-2">
            <template x-for="category in meme.state.templateCategories" :key="category.id">
              <button
                type="button"
                class="rounded-full border px-3 py-1 text-xs font-semibold transition"
                :class="
                  category.id === meme.state.selectedCategoryId
                    ? 'border-indigo-500 bg-indigo-50 text-indigo-600'
                    : 'border-slate-200 bg-white text-slate-600 hover:border-slate-300'
                "
                x-on:click="meme.selectCategory(category.id)"
              >
                <span x-text="category.title"></span>
              </button>
            </template>
          </div>
        </div>

        <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
          <h3 class="text-sm font-semibold text-slate-800">Trending tags</h3>
          <template x-if="meme.selectedCategory">
            <div class="mt-3 flex flex-wrap gap-2 text-xs text-indigo-600">
              <template x-for="tag in meme.selectedCategory?.tags ?? []" :key="tag">
                <span class="inline-flex items-center gap-1 rounded-full bg-indigo-50 px-3 py-1 font-semibold">
                  <i class="fas fa-hashtag text-[10px]"></i>
                  <span x-text="tag.replace('#', '')"></span>
                </span>
              </template>
            </div>
          </template>
          <template x-if="!meme.selectedCategory">
            <p class="mt-3 text-xs text-slate-500">Select a category to see trending tags.</p>
          </template>
        </div>

        <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
          <h3 class="text-sm font-semibold text-slate-800">Prompt recommendations</h3>
          <p class="mt-2 text-xs text-slate-500">
            Your current prompt preset pairs best with these templates. Swap tone or prompt to refresh suggestions.
          </p>
          <ul class="mt-3 space-y-3 text-sm text-slate-600">
            <template x-for="template in meme.recommendedTemplatesForPrompt()" :key="template.id">
              <li class="flex items-start gap-2">
                <i class="fas fa-sparkles mt-0.5 text-indigo-500"></i>
                <div>
                  <p class="font-semibold text-slate-800" x-text="template.name"></p>
                  <p class="text-xs text-slate-500" x-text="template.recommendedUse"></p>
                </div>
              </li>
            </template>
            <template x-if="!meme.recommendedTemplatesForPrompt().length">
              <li class="text-xs text-slate-500">Pick a prompt preset to view smart suggestions.</li>
            </template>
          </ul>
        </div>
      </aside>

      <div class="space-y-6">
        <template x-for="template in meme.templatesForSelectedCategory()" :key="template.id">
          <article
            class="rounded-3xl border p-6 transition hover:border-indigo-300 hover:shadow-lg"
            :class="
              template.id === meme.state.selectedTemplateId
                ? 'border-indigo-400 bg-indigo-50/60'
                : 'border-slate-200 bg-white'
            "
          >
            <div class="flex flex-wrap items-start justify-between gap-4">
              <div>
                <div class="flex items-center gap-2">
                  <h3 class="text-lg font-semibold text-slate-900" x-text="template.name"></h3>
                  <span
                    class="inline-flex items-center gap-1 rounded-full px-3 py-1 text-[10px] font-semibold uppercase tracking-wide"
                    :class="template.animated ? 'bg-purple-100 text-purple-600' : 'bg-slate-100 text-slate-600'"
                  >
                    <i class="fas fa-wave-square text-[9px]"></i>
                    <span x-text="template.animated ? 'Animated' : 'Static'"></span>
                  </span>
                </div>
                <p class="mt-2 text-sm text-slate-600" x-text="template.description"></p>
                <p class="mt-2 text-xs text-slate-500">
                  Use for:
                  <span class="font-medium text-slate-700" x-text="template.recommendedUse"></span>
                </p>
                <div class="mt-3 flex flex-wrap gap-2 text-[10px] text-slate-500">
                  <span class="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2 py-1 font-semibold">
                    <i class="fas fa-up-right-and-down-left-from-center"></i>
                    <span x-text="template.ratio"></span>
                  </span>
                  <span class="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2 py-1 font-semibold">
                    <i class="fas fa-table-cells-large"></i>
                    <span x-text="`${template.panels} panels`"></span>
                  </span>
                  <template x-for="tag in template.tags" :key="tag">
                    <span class="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2 py-1 font-semibold">
                      <i class="fas fa-tag text-[9px]"></i>
                      <span x-text="tag"></span>
                    </span>
                  </template>
                </div>
              </div>
              <div class="flex flex-col items-end gap-3">
                <button
                  type="button"
                  class="rounded-full border border-indigo-500 px-4 py-2 text-xs font-semibold text-indigo-600 transition hover:bg-indigo-500 hover:text-white"
                  x-on:click="meme.selectTemplate(template.id)"
                >
                  Set as active
                </button>
                <span
                  class="inline-flex items-center gap-2 rounded-full bg-indigo-100 px-3 py-1 text-[10px] font-semibold uppercase tracking-wide text-indigo-600"
                  x-show="meme.recommendedTemplatesForPrompt().some((item) => item.id === template.id)"
                >
                  <i class="fas fa-bolt text-[9px]"></i>
                  Prompt match
                </span>
              </div>
            </div>
          </article>
        </template>
        <template x-if="!meme.templatesForSelectedCategory().length">
          <p class="rounded-3xl border border-dashed border-slate-300 bg-slate-50 p-6 text-sm text-slate-500">
            No templates found. Try another category or adjust filters.
          </p>
        </template>
      </div>
    </div>
  </div>
</section>
