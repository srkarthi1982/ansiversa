---
import SectionHeading from '../../../components/SectionHeading.astro';
---
<section id="ai-meme-safety" class="border-t border-slate-200 bg-slate-50 py-16" x-data="() => ({ meme: $store.aiMemeCreator })">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <SectionHeading
      eyebrow="Safety & compliance"
      title="Keep memes on-brand with profanity filters, sensitive-topic guards, and watermark locks"
      description="Safety controls stay live in the workspace so brand teams can approve exports confidently. Toggle guardrails per plan and log overrides for audits."
    />

    <div class="mt-12 grid gap-10 lg:grid-cols-[1.1fr_0.9fr]">
      <div class="space-y-6">
        <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
          <div class="flex items-center justify-between">
            <h3 class="text-sm font-semibold text-slate-900">Safety controls</h3>
            <span class="inline-flex items-center gap-2 rounded-full bg-indigo-50 px-3 py-1 text-[10px] font-semibold uppercase tracking-wide text-indigo-600">
              <i class="fas fa-user-shield text-[9px]"></i>
              <span x-text="meme.state.plan.toUpperCase()"></span>
            </span>
          </div>
          <p class="mt-2 text-xs text-slate-500">
            Toggle controls to match your brand policy. Pro-only guardrails remain visible but locked on Free plan.
          </p>
          <div class="mt-4 space-y-3">
            <template x-for="control in meme.state.safetyControls" :key="control.id">
              <label
                class="flex w-full items-start gap-3 rounded-2xl border px-4 py-3 text-sm transition"
                :class="
                  meme.isSafetyControlActive(control.id)
                    ? 'border-indigo-500 bg-indigo-50 text-indigo-700'
                    : 'border-slate-200 bg-white text-slate-600 hover:border-slate-300'
                "
              >
                <input
                  type="checkbox"
                  class="mt-1 h-4 w-4 rounded border-slate-300 text-indigo-500 focus:ring-indigo-500"
                  :checked="meme.isSafetyControlActive(control.id)"
                  :disabled="control.defaultLevel === 'pro' && meme.state.plan !== 'pro'"
                  x-on:change="meme.toggleSafetyControl(control.id)"
                />
                <div>
                  <p class="font-semibold" x-text="control.label"></p>
                  <p class="text-xs" x-text="control.description"></p>
                  <div class="mt-2 flex flex-wrap gap-2 text-[10px] text-slate-500">
                    <template x-for="setting in control.settings" :key="setting">
                      <span class="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2 py-1">
                        <i class="fas fa-gear text-[9px]"></i>
                        <span x-text="setting"></span>
                      </span>
                    </template>
                  </div>
                </div>
                <span
                  class="ml-auto mt-1 inline-flex items-center gap-1 rounded-full px-3 py-1 text-[10px] font-semibold uppercase tracking-wide"
                  :class="control.defaultLevel === 'pro' ? 'bg-purple-100 text-purple-600' : 'bg-emerald-100 text-emerald-600'"
                >
                  <i class="fas fa-lock text-[9px]"></i>
                  <span x-text="control.defaultLevel === 'pro' ? 'Pro default' : 'Free default'"></span>
                </span>
              </label>
            </template>
          </div>
        </div>

        <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
          <div class="flex items-center justify-between">
            <div>
              <h3 class="text-sm font-semibold text-slate-900">Plan selector</h3>
              <p class="mt-1 text-xs text-slate-500">Preview guardrails on each tier.</p>
            </div>
            <div class="inline-flex rounded-full bg-slate-100 p-1 text-xs font-semibold text-slate-600">
              <button
                type="button"
                class="rounded-full px-3 py-1"
                :class="meme.state.plan === 'free' ? 'bg-white text-slate-900 shadow-sm' : ''"
                x-on:click="meme.setPlan('free')"
              >
                Free
              </button>
              <button
                type="button"
                class="rounded-full px-3 py-1"
                :class="meme.state.plan === 'pro' ? 'bg-indigo-600 text-white shadow-sm' : ''"
                x-on:click="meme.setPlan('pro')"
              >
                Pro
              </button>
            </div>
          </div>
          <p class="mt-4 text-xs text-slate-500">
            Audit logs capture who toggled safety settings, with timestamps synced to export jobs for compliance trails.
          </p>
        </div>
      </div>

      <div class="space-y-6">
        <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
          <h3 class="text-sm font-semibold text-slate-900">Alerts & resolutions</h3>
          <p class="mt-2 text-xs text-slate-500">When something needs review, we surface the issue and recommended fixes.</p>
          <div class="mt-4 space-y-3">
            <template x-for="alert in meme.state.safetyAlerts" :key="alert.id">
              <article class="rounded-2xl border border-slate-200 bg-slate-50 p-4">
                <div class="flex items-center justify-between gap-3">
                  <div>
                    <p class="font-semibold text-slate-900" x-text="alert.label"></p>
                    <p class="mt-1 text-sm text-slate-600" x-text="alert.description"></p>
                  </div>
                  <span
                    class="inline-flex items-center gap-2 rounded-full px-3 py-1 text-[10px] font-semibold uppercase tracking-wide"
                    :class="
                      alert.severity === 'high'
                        ? 'bg-rose-100 text-rose-600'
                        : alert.severity === 'medium'
                        ? 'bg-amber-100 text-amber-600'
                        : 'bg-emerald-100 text-emerald-600'
                    "
                  >
                    <i class="fas fa-circle-exclamation text-[9px]"></i>
                    <span x-text="alert.severity"></span>
                  </span>
                </div>
                <p class="mt-3 text-xs font-semibold text-indigo-600" x-text="alert.resolution"></p>
              </article>
            </template>
          </div>
        </div>

        <div class="rounded-3xl border border-dashed border-indigo-200 bg-indigo-50 p-6 text-sm text-indigo-700">
          <p class="font-semibold text-indigo-900">Face blur on upload</p>
          <p class="mt-2">
            Uploaded images trigger automatic face detection. Adjust blur strength or switch to manual brush mode directly in the canvas before exporting.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
