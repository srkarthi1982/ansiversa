---
import Button from "../../components/Button.astro";
---
<section class="space-y-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
  <header class="space-y-2">
    <div class="flex items-center justify-between gap-2">
      <h2 class="text-lg font-semibold text-slate-900">Idea explorer</h2>
      <span class="inline-flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">
        <i class="fas fa-lightbulb text-[10px]"></i>
        <span x-text="$store['blog-writer'].builder.aiUsage.ideas"></span>
        AI calls today
      </span>
    </div>
    <p class="text-sm text-slate-600">
      Feed a topic, audience, or outcome. Ansiversa suggests angles ranked by expertise level.
    </p>
  </header>
  <form
    class="space-y-3"
    @submit.prevent="$store['blog-writer'].generateIdeas()"
  >
    <label class="block text-xs font-semibold uppercase tracking-wide text-slate-500">Topic or goal</label>
    <input
      type="text"
      class="w-full rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm text-slate-900 shadow-sm focus:border-indigo-400 focus:outline-none focus:ring-2 focus:ring-indigo-100"
      placeholder="e.g. onboarding emails for SaaS"
      :value="$store['blog-writer'].builder.ideaTopic"
      @input="$store['blog-writer'].updateIdeaTopic($event.target.value)"
    />
    <Button type="submit" block :disabled="!$store['blog-writer'].builder.ideaTopic" :class="$store['blog-writer'].builder.ideaLoading ? 'animate-pulse' : ''">
      <i class="fas fa-wand-magic-sparkles text-xs"></i>
      <span x-text="$store['blog-writer'].builder.ideaLoading ? 'Generating…' : 'Generate ideas'"></span>
    </Button>
  </form>
  <div class="space-y-3">
    <template x-if="$store['blog-writer'].builder.ideas.length === 0">
      <p class="rounded-2xl border border-dashed border-slate-200 bg-slate-50 p-4 text-sm text-slate-500">
        Ideas appear here after you run the assistant. Use them to craft outlines instantly.
      </p>
    </template>
    <template x-for="idea in $store['blog-writer'].builder.ideas" :key="idea.id">
      <article
        class="space-y-2 rounded-2xl border border-slate-200 p-4 text-sm shadow-sm transition hover:border-indigo-200 hover:shadow-md"
        :class="$store['blog-writer'].builder.selectedIdeaId === idea.id ? 'border-indigo-300 bg-indigo-50/60' : 'bg-white'"
      >
        <div class="flex items-center justify-between gap-2">
          <h3 class="text-sm font-semibold text-slate-900" x-text="idea.headline"></h3>
          <span class="inline-flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1 text-[11px] font-semibold text-slate-600">
            <i class="fas fa-compass text-[9px]"></i>
            <span class="capitalize" x-text="idea.angle"></span>
          </span>
        </div>
        <p class="text-xs text-slate-600" x-text="idea.summary"></p>
        <div class="flex flex-wrap gap-2">
          <Button type="button" size="sm" variant="light" @click.prevent="$store['blog-writer'].chooseIdea(idea)">
            <i class="fas fa-check text-xs"></i>
            Use idea
          </Button>
          <Button type="button" size="sm" variant="ghost" @click.prevent="$store['blog-writer'].generateOutlineFromIdea()">
            <i class="fas fa-diagram-project text-xs"></i>
            Outline it
          </Button>
        </div>
      </article>
    </template>
  </div>
</section>
