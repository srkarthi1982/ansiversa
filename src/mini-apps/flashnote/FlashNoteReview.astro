---
---
<div
  x-data="() => ({ flashnote: $store.flashnote })"
  x-show="flashnote.state.reviewMode"
  x-transition.opacity
  class="fixed inset-0 z-40 flex items-center justify-center bg-slate-900/80 px-4 py-10"
  @keydown.window.prevent="
    if (!flashnote.state.reviewMode) return;
    if (event.key === 'Escape') { flashnote.toggleReview(false); }
    if (event.key === 'ArrowRight') { flashnote.nextCard(); }
    if (event.key === 'ArrowLeft') { flashnote.prevCard(); }
    if (event.key === ' ' || event.key === 'Spacebar') { flashnote.flipCard(); }
  "
>
  <div class="relative w-full max-w-4xl rounded-3xl bg-white p-8 shadow-2xl">
    <header class="flex flex-wrap items-center justify-between gap-4">
      <div>
        <span class="inline-flex items-center gap-2 rounded-full bg-indigo-50 px-3 py-1 text-xs font-semibold text-indigo-700">
          <i class="fas fa-bolt text-[10px]"></i>
          Study mode
        </span>
        <h2 class="mt-3 text-2xl font-semibold text-slate-900">Review deck</h2>
        <p class="text-sm text-slate-600">
          Use ← and → to navigate cards, spacebar to flip between prompt and answer.
        </p>
      </div>
      <button
        type="button"
        class="inline-flex items-center gap-2 rounded-full border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-700 shadow-sm transition hover:border-rose-200 hover:text-rose-600"
        @click="flashnote.toggleReview(false)"
      >
        <i class="fas fa-xmark text-xs"></i>
        Close
      </button>
    </header>

    <div class="mt-8">
      <template x-if="flashnote.state.reviewCards.length === 0">
        <div class="rounded-3xl border border-dashed border-slate-300 bg-slate-50 px-6 py-12 text-center text-sm text-slate-600">
          Add notes or adjust your filters to build a deck.
        </div>
      </template>

      <template x-if="flashnote.state.reviewCards.length > 0">
        <div class="space-y-6">
          <div class="flex items-center justify-between text-xs font-medium text-slate-500">
            <span>
              Card
              <span x-text="flashnote.state.reviewIndex + 1"></span>
              /
              <span x-text="flashnote.state.reviewCards.length"></span>
            </span>
            <div class="inline-flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1">
              <i class="fas fa-hashtag text-[10px]"></i>
              <span x-text="flashnote.state.filterTag || 'All tags'"></span>
            </div>
          </div>

          <div class="relative overflow-hidden rounded-3xl border border-slate-200 bg-gradient-to-br from-indigo-50 via-white to-slate-50 p-8 shadow-inner">
            <template x-for="(card, index) in flashnote.state.reviewCards" :key="`review-${card.id}`">
              <div
                x-show="index === flashnote.state.reviewIndex"
                x-transition.opacity
                class="space-y-4"
              >
                <h3 class="text-xl font-semibold text-slate-900" x-text="card.title"></h3>
                <p class="text-sm text-slate-600" x-show="!flashnote.state.reviewFlipped" x-text="card.content"></p>
                <div x-show="flashnote.state.reviewFlipped" class="rounded-2xl bg-white/70 p-4 text-sm text-slate-700 shadow-sm">
                  <h4 class="text-xs font-semibold uppercase tracking-wide text-slate-500">Answer</h4>
                  <p class="mt-2 whitespace-pre-line" x-text="card.summary || card.content"></p>
                </div>
                <div class="flex flex-wrap gap-2">
                  <template x-for="tag in card.tags" :key="`${card.id}-tag-${tag}`">
                    <span class="inline-flex items-center gap-2 rounded-full bg-indigo-100 px-3 py-1 text-xs font-semibold text-indigo-700">
                      <i class="fas fa-tag text-[10px]"></i>
                      <span x-text="tag"></span>
                    </span>
                  </template>
                </div>
              </div>
            </template>
          </div>

          <div class="flex flex-wrap items-center justify-between gap-3">
            <div class="flex flex-wrap items-center gap-3">
              <button
                type="button"
                class="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-700 shadow-sm transition hover:border-indigo-200 hover:text-indigo-600"
                @click="flashnote.prevCard()"
              >
                <i class="fas fa-arrow-left text-xs"></i>
                Previous
              </button>
              <button
                type="button"
                class="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-700 shadow-sm transition hover:border-indigo-200 hover:text-indigo-600"
                @click="flashnote.flipCard()"
              >
                <i class="fas fa-repeat text-xs"></i>
                Flip
              </button>
              <button
                type="button"
                class="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-700 shadow-sm transition hover:border-indigo-200 hover:text-indigo-600"
                @click="flashnote.nextCard()"
              >
                Next
                <i class="fas fa-arrow-right text-xs"></i>
              </button>
            </div>
            <p class="text-xs text-slate-500">Shortcuts: ← previous, → next, space to flip, Esc to exit.</p>
          </div>
        </div>
      </template>
    </div>
  </div>
</div>
