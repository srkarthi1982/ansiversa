---
import Button from "../Button.astro";
import SectionHeading from "../SectionHeading.astro";
---
<section class="bg-slate-100 py-16" x-data="() => ({ novel: $store['novel-outliner'] })">
  <div class="mx-auto max-w-6xl space-y-10 px-4 sm:px-6 lg:px-8">
    <SectionHeading
      eyebrow="Snowflake workspace"
      title="Layer ideas from logline to chapter grid"
      description="Track progress across every Snowflake stage. Free mode unlocks the core three steps, while Pro enables advanced branching, chapter packaging, and continuity sweeps."
    />
    <div class="grid gap-8 lg:grid-cols-[0.9fr_1.1fr]">
      <div class="rounded-3xl border border-white bg-white p-6 shadow-sm">
        <div class="flex items-center justify-between">
          <h3 class="text-lg font-semibold text-slate-900">Stage tracker</h3>
          <span class="inline-flex items-center gap-2 rounded-full bg-indigo-100 px-3 py-1 text-[11px] font-semibold text-indigo-700">
            <i class="fa-solid fa-chart-line text-[10px]"></i>
            <span x-text="`${novel.snowflakeCompletionPercent}% complete`"></span>
          </span>
        </div>
        <ol class="mt-4 space-y-3">
          <template x-for="stage in novel.state.snowflakeStages" :key="stage.step">
            <li
              class="rounded-2xl border px-4 py-3"
              :class="[
                stage.step === novel.state.selectedSnowflakeStep ? 'border-indigo-300 bg-indigo-50' : 'border-slate-200 bg-slate-50',
                stage.proOnly && novel.plan === 'free' ? 'opacity-60' : 'opacity-100'
              ]"
            >
              <div class="flex items-start justify-between gap-3">
                <div>
                  <p class="text-sm font-semibold text-slate-900">
                    <span class="text-indigo-600">
                      Step <span x-text="stage.step"></span> ·
                    </span>
                    <span x-text="stage.label"></span>
                  </p>
                  <p class="mt-1 text-xs text-slate-500" x-text="stage.description"></p>
                </div>
                <span
                  class="inline-flex items-center gap-2 rounded-full px-3 py-1 text-[10px] font-semibold uppercase tracking-[0.3em]"
                  :class="stage.proOnly ? 'bg-rose-100 text-rose-600' : 'bg-emerald-100 text-emerald-700'"
                >
                  <i class="fa-solid" :class="stage.proOnly ? 'fa-lock text-[9px]' : 'fa-circle-check text-[9px]'"></i>
                  <span x-text="stage.proOnly ? 'Pro' : 'Free'"></span>
                </span>
              </div>
            </li>
          </template>
        </ol>
        <div class="mt-5 flex items-center gap-3">
          <Button type="button" variant="outline" class="border-slate-300 text-slate-700" @click.prevent="novel.rewindSnowflake()">
            <i class="fa-solid fa-arrow-left text-xs"></i>
            Previous
          </Button>
          <Button type="button" class="shadow-sm" @click.prevent="novel.advanceSnowflake()">
            <i class="fa-solid fa-arrow-right text-xs"></i>
            Next unlocked step
          </Button>
        </div>
        <p class="mt-3 text-xs text-slate-500">Free plans can progress through step <span class="font-semibold text-slate-700" x-text="novel.allowedSnowflakeSteps"></span>. Pro unlocks full 10-step flow with branching.</p>
      </div>
      <div class="space-y-5">
        <template x-if="novel.selectedSnowflake">
          <div class="rounded-3xl border border-indigo-200 bg-white p-6 shadow-sm">
            <div class="flex flex-wrap items-center justify-between gap-3">
              <div>
                <p class="text-xs font-semibold uppercase tracking-[0.3em] text-indigo-500">Currently focused</p>
                <h3 class="text-2xl font-semibold text-slate-900" x-text="novel.selectedSnowflake.label"></h3>
              </div>
              <div class="rounded-2xl border border-indigo-200 bg-indigo-50 px-4 py-3 text-xs text-indigo-600">
                <p class="font-semibold">Deliverable</p>
                <p class="mt-1 font-semibold text-indigo-700" x-text="novel.selectedSnowflake.deliverable"></p>
              </div>
            </div>
            <p class="mt-4 text-sm text-slate-600" x-text="novel.selectedSnowflake.description"></p>
            <div class="mt-4">
              <p class="text-xs font-semibold uppercase tracking-[0.3em] text-slate-500">Inputs to cover</p>
              <div class="mt-2 flex flex-wrap gap-2">
                <template x-for="input in novel.selectedSnowflake.inputs" :key="input">
                  <span class="inline-flex items-center gap-2 rounded-full border border-indigo-200 bg-indigo-50 px-3 py-1 text-[11px] font-medium text-indigo-700">
                    <i class="fa-solid fa-circle-dot text-[8px]"></i>
                    <span x-text="input"></span>
                  </span>
                </template>
              </div>
            </div>
          </div>
        </template>
        <div class="grid gap-5 md:grid-cols-2">
          <div class="rounded-3xl border border-white bg-white p-5 shadow-sm">
            <h4 class="text-base font-semibold text-slate-900">Scene lanes</h4>
            <p class="mt-1 text-xs text-slate-500">Kanban view from idea to revised draft keeps planner momentum visible.</p>
            <ul class="mt-3 space-y-2 text-xs text-slate-600">
              <template x-for="lane in novel.state.sceneBoard" :key="lane.key">
                <li class="rounded-2xl border border-slate-200 bg-slate-50 px-3 py-3">
                  <div class="flex items-center justify-between">
                    <span class="font-semibold text-slate-900" x-text="lane.label"></span>
                    <span class="text-[11px] font-semibold uppercase tracking-[0.3em] text-indigo-500" x-text="`${lane.scenes.length} scenes`"></span>
                  </div>
                  <p class="mt-1" x-text="lane.description"></p>
                </li>
              </template>
            </ul>
          </div>
          <div class="rounded-3xl border border-white bg-white p-5 shadow-sm">
            <h4 class="text-base font-semibold text-slate-900">Revision snapshots</h4>
            <p class="mt-1 text-xs text-slate-500">Compare key passes, track diagnostics, and keep a shareable change log.</p>
            <ul class="mt-3 space-y-2 text-xs text-slate-600">
              <template x-for="snapshot in novel.state.revisionSnapshots" :key="snapshot.id">
                <li class="rounded-2xl border border-slate-200 bg-slate-50 px-3 py-3">
                  <p class="font-semibold text-slate-900" x-text="snapshot.label"></p>
                  <p class="mt-1" x-text="snapshot.summary"></p>
                  <div class="mt-2 flex flex-wrap gap-2">
                    <template x-for="highlight in snapshot.highlights" :key="highlight">
                      <span class="inline-flex items-center gap-2 rounded-full border border-indigo-200 bg-indigo-50 px-3 py-1 text-[10px] font-medium text-indigo-600">
                        <i class="fa-solid fa-sparkles text-[8px]"></i>
                        <span x-text="highlight"></span>
                      </span>
                    </template>
                  </div>
                </li>
              </template>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
