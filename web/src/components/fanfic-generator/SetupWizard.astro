---
import Button from "../Button.astro";
import SectionHeading from "../SectionHeading.astro";
---
<section
  id="fanfic-setup"
  class="bg-slate-100 py-16 text-slate-900 sm:py-20"
  x-data="() => ({ fanfic: $store['fanfic-generator'] })"
>
  <div class="mx-auto max-w-6xl space-y-12 px-4 sm:px-6 lg:px-8">
    <SectionHeading
      eyebrow="Canon-safe onboarding"
      title="Spin up a transformative project in minutes"
      description="Capture canon notes, apply AU presets, and lock in consent-friendly ships before drafting a single scene."
    />
    <div
      x-show="fanfic.view.showWizard"
      x-transition
      class="rounded-3xl border border-slate-200 bg-white shadow-soft"
    >
      <div class="border-b border-slate-200 px-6 py-4 flex flex-wrap items-center justify-between gap-3">
        <div>
          <p class="text-sm font-semibold text-slate-900">Canon setup wizard</p>
          <p class="text-xs text-slate-500">All changes autosave to the draft project preview.</p>
        </div>
        <div class="flex items-center gap-2">
          <Button type="button" variant="ghost" class="text-slate-600 hover:text-slate-900" @click.prevent="fanfic.resetWizard()">
            <i class="fa-solid fa-rotate-left text-xs"></i>
            Reset fields
          </Button>
          <Button type="button" @click.prevent="fanfic.createProjectFromWizard()">
            <i class="fa-solid fa-wand-magic-sparkles text-xs"></i>
            Generate project draft
          </Button>
        </div>
      </div>
      <div class="grid gap-6 px-6 py-8 lg:grid-cols-[1.05fr_0.95fr]">
        <div class="space-y-6">
          <div class="space-y-3">
            <label class="block text-sm font-semibold text-slate-700">Fandom & working title</label>
            <div class="grid gap-3 sm:grid-cols-2">
              <div>
                <p class="text-xs font-semibold uppercase tracking-[0.25em] text-indigo-500">Fandom</p>
                <input
                  type="text"
                  x-model="fanfic.state.wizard.fandom"
                  class="mt-1 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 text-sm shadow-inner focus:border-indigo-500 focus:outline-none"
                />
              </div>
              <div>
                <p class="text-xs font-semibold uppercase tracking-[0.25em] text-indigo-500">Title</p>
                <input
                  type="text"
                  x-model="fanfic.state.wizard.title"
                  class="mt-1 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 text-sm shadow-inner focus:border-indigo-500 focus:outline-none"
                />
              </div>
            </div>
          </div>
          <div class="space-y-3">
            <label class="block text-sm font-semibold text-slate-700">Canon anchors</label>
            <textarea
              x-model="fanfic.state.wizard.canonNotes"
              rows="4"
              class="w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 text-sm shadow-inner focus:border-indigo-500 focus:outline-none"
              placeholder="Season fixes, characters to protect, arcs to preserve"
            ></textarea>
          </div>
          <div class="space-y-4 rounded-2xl border border-slate-200 bg-slate-50/70 p-4">
            <div class="flex items-center justify-between">
              <p class="text-sm font-semibold text-slate-800">AU preset</p>
              <span class="inline-flex items-center gap-1 rounded-full bg-indigo-500/10 px-3 py-1 text-[11px] font-semibold text-indigo-500">
                <i class="fa-solid fa-layer-group text-[10px]"></i>
                Mix & match presets
              </span>
            </div>
            <p class="text-xs text-slate-600">Select a preset to auto-fill setting conflicts and tone cues.</p>
            <div class="grid gap-3 sm:grid-cols-2">
              <template x-for="preset in fanfic.auPresets" :key="preset.id">
                <button
                  type="button"
                  class="rounded-2xl border border-slate-200 bg-white px-4 py-3 text-left text-sm shadow-sm transition hover:border-indigo-400 hover:shadow-md"
                  :class="fanfic.state.wizard.auPresetId === preset.id ? 'border-indigo-500 ring-2 ring-indigo-200' : ''"
                  @click="fanfic.applyAuPreset(preset.id)"
                >
                  <div class="flex items-center justify-between gap-2">
                    <p class="font-semibold text-slate-900" x-text="preset.label"></p>
                    <span class="text-[10px] font-semibold uppercase tracking-[0.25em]" :class="preset.plan === 'pro' ? 'text-amber-500' : 'text-emerald-500'" x-text="preset.plan === 'pro' ? 'Pro' : 'Free'"></span>
                  </div>
                  <p class="mt-1 text-xs text-slate-600" x-text="preset.description"></p>
                </button>
              </template>
            </div>
            <textarea
              x-model="fanfic.state.wizard.customSetting"
              rows="4"
              class="w-full rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm shadow-inner focus:border-indigo-500 focus:outline-none"
              placeholder="Describe how the AU shifts canon rules"
            ></textarea>
          </div>
          <div class="space-y-3">
            <p class="text-sm font-semibold text-slate-700">Tropes & themes</p>
            <div class="flex flex-wrap gap-2">
              <template x-for="trope in fanfic.state.wizard.tropes" :key="trope">
                <span class="inline-flex items-center gap-2 rounded-full bg-indigo-500/10 px-3 py-1 text-xs font-semibold text-indigo-500">
                  <i class="fa-solid fa-tag text-[10px]"></i>
                  <span x-text="trope"></span>
                </span>
              </template>
            </div>
            <div class="grid gap-2 sm:grid-cols-2">
              <template x-for="bundle in fanfic.tropeBundles" :key="bundle.id">
                <button
                  type="button"
                  class="rounded-xl border border-dashed border-indigo-200 bg-white px-4 py-3 text-left text-xs text-slate-600 transition hover:border-indigo-400 hover:text-indigo-500"
                  @click="fanfic.applyTropeBundle(bundle.id)"
                >
                  <p class="font-semibold text-slate-700" x-text="bundle.label"></p>
                  <p class="mt-1" x-text="bundle.description"></p>
                </button>
              </template>
            </div>
          </div>
        </div>
        <div class="space-y-6">
          <div class="grid gap-3 sm:grid-cols-2">
            <div>
              <label class="block text-sm font-semibold text-slate-700">Rating</label>
              <select
                x-model="fanfic.state.wizard.rating"
                class="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none"
              >
                <option value="G">General (G)</option>
                <option value="T">Teen (T)</option>
                <option value="M">Mature (M)</option>
              </select>
            </div>
            <div>
              <label class="block text-sm font-semibold text-slate-700">Target words</label>
              <input
                type="number"
                min="1000"
                step="500"
                x-model.number="fanfic.state.wizard.wordTarget"
                class="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none"
              />
            </div>
          </div>
          <div>
            <label class="block text-sm font-semibold text-slate-700">Warnings</label>
            <input
              type="text"
              :value="fanfic.state.wizard.warnings.join(', ')"
              class="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none"
              @change="fanfic.updateWizardField('warnings', $event.target.value.split(',').map((item) => item.trim()).filter(Boolean))"
              placeholder="violence, injury"
            />
            <p class="mt-1 text-xs text-slate-500">Comma separated list. Safety checks auto-validate consent and age gates.</p>
          </div>
          <div class="space-y-4">
            <div class="flex items-center justify-between">
              <p class="text-sm font-semibold text-slate-700">Pairings & dynamics</p>
              <Button type="button" size="sm" variant="secondary" class="bg-indigo-600 text-white hover:bg-indigo-500" @click.prevent="fanfic.addWizardPairing()">
                <i class="fa-solid fa-plus text-[10px]"></i>
                Add pairing
              </Button>
            </div>
            <div class="space-y-3">
              <template x-for="(pairing, index) in fanfic.state.wizard.pairings" :key="`${pairing.label}-${index}`">
                <div class="rounded-2xl border border-slate-200 bg-white px-4 py-3 shadow-sm">
                  <div class="flex items-start justify-between gap-3">
                    <div class="space-y-2 flex-1">
                      <input
                        type="text"
                        x-model="pairing.label"
                        class="w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none"
                        placeholder="Pairing label"
                      />
                      <textarea
                        x-model="pairing.dynamic"
                        rows="2"
                        class="w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs focus:border-indigo-500 focus:outline-none"
                        placeholder="Dynamics, tropes, canon notes"
                      ></textarea>
                    </div>
                    <button
                      type="button"
                      class="rounded-full border border-slate-200 bg-white p-2 text-slate-500 transition hover:border-rose-400 hover:text-rose-500"
                      @click="fanfic.removeWizardPairing(index)"
                    >
                      <i class="fa-solid fa-xmark text-xs"></i>
                    </button>
                  </div>
                  <div class="mt-3 grid gap-2 sm:grid-cols-3">
                    <select
                      x-model="pairing.type"
                      class="rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs focus:border-indigo-500 focus:outline-none"
                    >
                      <option value="romantic">Romantic ship</option>
                      <option value="friendship">Friendship</option>
                      <option value="ensemble">Ensemble</option>
                    </select>
                    <label class="inline-flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-600">
                      <input type="checkbox" class="rounded border-slate-300 text-indigo-600" x-model="pairing.adultsOnly" />
                      Adults 18+
                    </label>
                    <label class="inline-flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-600">
                      <input type="checkbox" class="rounded border-slate-300 text-indigo-600" x-model="pairing.consentOk" />
                      Consent checked
                    </label>
                  </div>
                </div>
              </template>
            </div>
          </div>
          <div class="space-y-4 rounded-2xl border border-indigo-100 bg-indigo-50/60 p-4">
            <p class="text-sm font-semibold text-slate-700">Tone palette</p>
            <div class="grid gap-4 sm:grid-cols-2">
              <template x-for="field in ['fluff','angst','humor','suspense']" :key="field">
                <label class="space-y-2 text-xs font-semibold uppercase tracking-[0.25em] text-indigo-500">
                  <span x-text="field"></span>
                  <input
                    type="range"
                    min="0"
                    max="3"
                    :value="fanfic.state.wizard.tone[field]"
                    @input="fanfic.updateWizardToneField(field, Number($event.target.value))"
                    class="w-full accent-indigo-500"
                  />
                  <span class="block text-[11px] text-slate-600" x-text="`Level ${fanfic.state.wizard.tone[field]}`"></span>
                </label>
              </template>
            </div>
            <div class="grid gap-3 sm:grid-cols-2">
              <div>
                <label class="text-xs font-semibold uppercase tracking-[0.25em] text-indigo-500">POV</label>
                <input
                  type="text"
                  x-model="fanfic.state.wizard.tone.pov"
                  class="mt-1 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none"
                />
              </div>
              <div>
                <label class="text-xs font-semibold uppercase tracking-[0.25em] text-indigo-500">Tense</label>
                <input
                  type="text"
                  x-model="fanfic.state.wizard.tone.tense"
                  class="mt-1 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none"
                />
              </div>
            </div>
            <div>
              <label class="text-xs font-semibold uppercase tracking-[0.25em] text-indigo-500">Chapters planned</label>
              <input
                type="number"
                min="1"
                x-model.number="fanfic.state.wizard.chapters"
                class="mt-1 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none"
              />
            </div>
          </div>
          <div>
            <label class="block text-sm font-semibold text-slate-700">Project summary</label>
            <textarea
              x-model="fanfic.state.wizard.summary"
              rows="4"
              class="mt-2 w-full rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none"
              placeholder="Elevator pitch, stakes, promised payoff"
            ></textarea>
            <p class="mt-1 text-xs text-slate-500">Summaries feed canon-safe prompt context for outline generation.</p>
          </div>
        </div>
      </div>
    </div>
    <div
      x-show="!fanfic.view.showWizard"
      class="flex flex-col items-center justify-center gap-3 rounded-3xl border border-dashed border-indigo-300 bg-indigo-50/60 px-6 py-10 text-center"
    >
      <p class="text-base font-semibold text-slate-800">Wizard tucked away</p>
      <p class="max-w-xl text-sm text-slate-600">Use the hero call-to-action to reopen the canon setup wizard when you&apos;re ready to spin up a new project.</p>
      <Button type="button" variant="secondary" class="bg-indigo-600 text-white hover:bg-indigo-500" @click.prevent="fanfic.view.showWizard = true">
        <i class="fa-solid fa-sparkles text-xs"></i>
        Relaunch wizard
      </Button>
    </div>
  </div>
</section>
