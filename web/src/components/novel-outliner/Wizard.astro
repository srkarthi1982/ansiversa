---
import Button from "../Button.astro";
import SectionHeading from "../SectionHeading.astro";
---
<section class="bg-slate-50 py-16" x-data="() => ({ novel: $store['novel-outliner'] })">
  <div class="mx-auto max-w-6xl space-y-10 px-4 sm:px-6 lg:px-8">
    <SectionHeading
      eyebrow="Project wizard"
      title="Spin up a novel workspace in under two minutes"
      description="Capture the genre, audience, structure, and pitch DNA. The wizard scaffolds beats, snowflake steps, and AI prompts tailored to your goals."
    />
    <div class="grid gap-8 lg:grid-cols-[1.05fr_0.95fr]">
      <form class="space-y-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
        <div class="grid gap-4 sm:grid-cols-2">
          <label class="space-y-2 text-sm font-medium text-slate-700">
            Genre
            <input
              type="text"
              class="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-200"
              x-model="novel.state.wizard.genre"
              placeholder="Science Fantasy"
            />
          </label>
          <label class="space-y-2 text-sm font-medium text-slate-700">
            Audience
            <select
              class="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-200"
              x-model="novel.state.wizard.audience"
            >
              <option>Adult</option>
              <option>New Adult</option>
              <option>Young Adult</option>
              <option>Middle Grade</option>
            </select>
          </label>
          <label class="space-y-2 text-sm font-medium text-slate-700">
            Tone and comps
            <input
              type="text"
              class="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-200"
              x-model="novel.state.wizard.tone"
              placeholder="Lyrical &amp; high-stakes"
            />
          </label>
          <label class="space-y-2 text-sm font-medium text-slate-700">
            POV strategy
            <select
              class="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-200"
              x-model="novel.state.wizard.pov"
            >
              <option>First-person</option>
              <option>Limited third</option>
              <option>Multi-POV</option>
              <option>Omniscient</option>
            </select>
          </label>
          <label class="space-y-2 text-sm font-medium text-slate-700">
            Tense
            <select
              class="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-200"
              x-model="novel.state.wizard.tense"
            >
              <option>Past</option>
              <option>Present</option>
              <option>Future</option>
            </select>
          </label>
          <label class="space-y-2 text-sm font-medium text-slate-700">
            Target word count
            <input
              type="number"
              min="10000"
              step="5000"
              class="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-200"
              x-model.number="novel.state.wizard.wordTarget"
            />
          </label>
        </div>
        <label class="space-y-2 text-sm font-medium text-slate-700">
          Elevator pitch / logline
          <textarea
            rows="3"
            class="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-200"
            placeholder="When a chronomancer fractures reality, an archivist must repair the timeline before memory itself unravels."
            x-model="novel.state.wizard.elevatorPitch"
          ></textarea>
        </label>
        <div class="grid gap-4 lg:grid-cols-[0.65fr_0.35fr]">
          <label class="space-y-2 text-sm font-medium text-slate-700">
            Structure preset
            <select
              class="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-200"
              x-model="novel.state.wizard.structureKey"
            >
              <template x-for="preset in novel.state.structurePresets" :key="preset.key">
                <option
                  :value="preset.key"
                  :disabled="preset.plan === 'pro' && novel.plan === 'free'"
                  x-text="preset.plan === 'pro' ? `${preset.name} · Pro` : preset.name"
                ></option>
              </template>
            </select>
          </label>
          <div class="rounded-2xl border border-dashed border-indigo-200 bg-indigo-50/60 p-4 text-xs text-indigo-700">
            <p class="font-semibold">Plan gating</p>
            <p class="mt-1" x-show="novel.plan === 'free'">Upgrade to unlock Save the Cat!, Hero&apos;s Journey, Kishōtenketsu, and Mystery Spine templates.</p>
            <p class="mt-1" x-show="novel.plan === 'pro'">All structure presets, diagnostics automation, and exports are active.</p>
          </div>
        </div>
        <div class="flex flex-wrap items-center justify-between gap-4">
          <div>
            <p class="text-xs font-semibold uppercase tracking-[0.3em] text-indigo-500">Preview summary</p>
            <p class="mt-1 text-sm text-slate-600">
              <span class="font-semibold" x-text="novel.state.wizard.genre"></span>
              · <span x-text="novel.state.wizard.audience"></span>
              · <span x-text="novel.state.wizard.pov"></span>
              · <span x-text="novel.state.wizard.structureKey"></span>
              · <span x-text="`${novel.state.wizard.wordTarget.toLocaleString()} words`"></span>
            </p>
          </div>
          <Button type="button" class="shadow-sm" @click.prevent="novel.queueProjectFromWizard()">
            <i class="fa-solid fa-plus text-xs"></i>
            Create project workspace
          </Button>
        </div>
      </form>
      <aside class="space-y-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
        <div class="flex items-center justify-between">
          <h3 class="text-lg font-semibold text-slate-900">Wizard guidance</h3>
          <span class="inline-flex items-center gap-2 rounded-full bg-indigo-100 px-3 py-1 text-[11px] font-semibold text-indigo-700">
            <i class="fa-solid fa-wand-magic-sparkles text-[10px]"></i>
            AI hints
          </span>
        </div>
        <ul class="space-y-3 text-sm text-slate-600">
          <li class="flex gap-3 rounded-2xl border border-slate-200/70 bg-slate-50 px-4 py-3">
            <i class="fa-solid fa-bullseye text-indigo-500"></i>
            <div>
              <p class="font-semibold text-slate-900">Structure-fit recommendations</p>
              <p class="text-xs text-slate-500">Novel Outliner analyses your comps and suggests pacing templates plus beat emphasis.</p>
            </div>
          </li>
          <li class="flex gap-3 rounded-2xl border border-slate-200/70 bg-slate-50 px-4 py-3">
            <i class="fa-solid fa-people-group text-indigo-500"></i>
            <div>
              <p class="font-semibold text-slate-900">Cast balance alerts</p>
              <p class="text-xs text-slate-500">Ensure POV rotation, gender balance, and relationship arcs align with your target audience.</p>
            </div>
          </li>
          <li class="flex gap-3 rounded-2xl border border-slate-200/70 bg-slate-50 px-4 py-3">
            <i class="fa-solid fa-code-branch text-indigo-500"></i>
            <div>
              <p class="font-semibold text-slate-900">Snowflake branching</p>
              <p class="text-xs text-slate-500">Branch alternate Act II or ending explorations without losing your primary outline.</p>
            </div>
          </li>
        </ul>
        <div class="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-xs text-slate-500">
          <p class="font-semibold text-slate-700">Export queue</p>
          <p class="mt-1" x-text="novel.state.exportState.status === 'idle' ? 'No exports queued yet.' : `Job ${novel.state.exportState.lastJobId} · ${novel.state.exportState.status}`"></p>
        </div>
        <div>
          <h4 class="text-sm font-semibold text-slate-900">Recent activity</h4>
          <ul class="mt-2 space-y-2 text-xs text-slate-600">
            <template x-for="event in novel.state.activity" :key="event.id">
              <li class="flex items-start gap-2 rounded-2xl border border-slate-200 bg-white px-3 py-2">
                <i class="fa-solid" :class="[event.icon, event.color, 'text-[11px] mt-0.5']"></i>
                <div>
                  <p class="font-semibold text-slate-900" x-text="event.label"></p>
                  <p x-text="event.detail"></p>
                  <p class="mt-1 text-[10px] uppercase tracking-[0.3em] text-slate-400" x-text="event.timestamp"></p>
                </div>
              </li>
            </template>
          </ul>
        </div>
      </aside>
    </div>
  </div>
</section>
