---
import { Button } from '@ansiversa/components';
---
<section class="space-y-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
  <header class="flex flex-wrap items-center justify-between gap-3">
    <div>
      <h2 class="text-xl font-semibold text-slate-900">Batch generator</h2>
      <p class="text-sm text-slate-600">Upload a CSV of payloads to queue printable ZIP archives with per-row status updates.</p>
    </div>
    <div class="inline-flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">
      <i class="fas fa-table text-[10px]"></i>
      Up to 50 rows per batch in preview
    </div>
  </header>

  <div class="space-y-4">
    <label class="block space-y-2 text-xs text-slate-600">
      <span class="font-semibold uppercase tracking-wide">CSV input</span>
      <textarea
        rows="6"
        class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-700 shadow-sm focus:border-indigo-500 focus:outline-none"
        placeholder="type,label,value,notes"
        :value="$store['qr-code-creator'].state.batch.csvText"
        @input="$store['qr-code-creator'].state.batch.csvText = $event.target.value"
      ></textarea>
      <p class="text-[11px] text-slate-500">Columns supported: type, label, value, notes. Use “|” to separate fields inside value for complex payloads (e.g., Wi-Fi or contact).</p>
    </label>
    <div class="flex flex-wrap items-center gap-3">
      <Button type="button" @click.prevent="$store['qr-code-creator'].processBatch()" :disabled="$store['qr-code-creator'].state.batch.processing">
        <i class="fas fa-gears text-xs"></i>
        <span x-text="$store['qr-code-creator'].state.batch.processing ? 'Processing…' : 'Generate batch'"></span>
      </Button>
      <template x-if="$store['qr-code-creator'].state.batch.downloadUrl">
        <a
          class="inline-flex items-center gap-2 rounded-full border border-indigo-200 bg-indigo-50 px-4 py-2 text-xs font-semibold text-indigo-600 shadow-sm hover:border-indigo-300"
          :href="$store['qr-code-creator'].state.batch.downloadUrl"
          download="ansiversa-qr-batch.zip"
        >
          <i class="fas fa-download text-[10px]"></i>
          Download ZIP
        </a>
      </template>
      <template x-if="$store['qr-code-creator'].state.batch.error">
        <span class="text-xs font-semibold text-red-600" x-text="$store['qr-code-creator'].state.batch.error"></span>
      </template>
    </div>
  </div>

  <div class="rounded-2xl border border-slate-200 bg-slate-50/70 p-4">
    <h3 class="text-sm font-semibold text-slate-900">Batch status</h3>
    <template x-if="$store['qr-code-creator'].state.batch.results.length === 0">
      <p class="mt-2 rounded-2xl border border-dashed border-slate-300 bg-white px-3 py-2 text-xs text-slate-500">
        Run the generator to view per-row progress and download the resulting archive.
      </p>
    </template>
    <div class="mt-3 space-y-2">
      <template x-for="result in $store['qr-code-creator'].state.batch.results" :key="result.id">
        <div class="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-white bg-white px-4 py-2 text-xs text-slate-600">
          <div>
            <p class="font-semibold text-slate-900" x-text="`${result.rowNumber}. ${result.label}`"></p>
            <p class="text-[11px] text-slate-500" x-text="result.fileName ?? '—'"></p>
          </div>
          <div class="flex items-center gap-2 text-[11px] font-semibold uppercase">
            <span
              class="inline-flex items-center gap-1 rounded-full px-3 py-1"
              :class="{
                'bg-emerald-100 text-emerald-600': result.status === 'success',
                'bg-amber-100 text-amber-600': result.status === 'processing',
                'bg-rose-100 text-rose-600': result.status === 'error',
                'bg-slate-100 text-slate-500': result.status === 'pending',
              }"
            >
              <i class="fas" :class="{
                'fa-circle-check': result.status === 'success',
                'fa-gear': result.status === 'processing',
                'fa-circle-xmark': result.status === 'error',
                'fa-hourglass': result.status === 'pending'
              }"></i>
              <span x-text="result.status"></span>
            </span>
            <template x-if="result.message">
              <span class="text-[11px] text-rose-600" x-text="result.message"></span>
            </template>
          </div>
        </div>
      </template>
    </div>
  </div>
</section>
