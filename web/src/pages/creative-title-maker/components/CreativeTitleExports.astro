---
import { SectionHeading, Button } from '@ansiversa/components';
---
<section class="border-t border-slate-200 bg-white py-16" x-data="() => ({ maker: $store.creativeTitleMaker })">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <SectionHeading
      eyebrow="Exports"
      title="Share variants as CSV, JSON, or PDF packs in one click"
      description="Queue export jobs with rationale, scoring, locales, and approvals baked in. Push straight to your favorite Ansiversa mini apps or download for stakeholders."
    />

    <div class="mt-10 grid gap-10 lg:grid-cols-[1.1fr_0.9fr]">
      <article class="rounded-3xl border border-slate-200 bg-slate-50 p-6 shadow-inner">
        <h3 class="text-lg font-semibold text-slate-900">Export presets</h3>
        <p class="mt-1 text-sm text-slate-600">Pick the delivery format that fits your workflow.</p>
        <div class="mt-5 space-y-4">
          <template x-for="preset in maker.state.exportPresets" :key="preset.id">
            <button
              type="button"
              class="w-full rounded-2xl border border-slate-200 bg-white p-5 text-left shadow-sm transition hover:-translate-y-0.5 hover:border-indigo-200"
              :class="maker.state.selectedExportId === preset.id ? 'border-indigo-300 shadow-md' : ''"
              @click.prevent="maker.selectExport(preset.id)"
            >
              <div class="flex flex-wrap items-center justify-between gap-3">
                <div>
                  <p class="text-base font-semibold text-slate-900" x-text="preset.label"></p>
                  <p class="text-xs text-slate-500" x-text="preset.description"></p>
                </div>
                <span class="inline-flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1 text-[11px] font-semibold text-slate-600">
                  <i class="fas fa-file-export text-[10px] text-indigo-500"></i>
                  <span x-text="preset.format.toUpperCase()"></span>
                </span>
              </div>
              <div class="mt-3 flex flex-wrap gap-2 text-[11px] font-semibold text-slate-500">
                <template x-for="field in preset.includes" :key="`${preset.id}-${field}`">
                  <span class="inline-flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1">
                    <i class="fas fa-circle-check text-[10px] text-indigo-500"></i>
                    <span x-text="field"></span>
                  </span>
                </template>
              </div>
              <div class="mt-4 flex items-center justify-between text-xs text-slate-500">
                <span class="font-semibold" x-text="preset.bestFor"></span>
                <span class="inline-flex items-center gap-2 rounded-full px-3 py-1"
                  :class="preset.plan === 'pro' ? 'bg-indigo-100 text-indigo-600' : 'bg-emerald-100 text-emerald-600'"
                >
                  <i class="fas fa-bolt text-[10px]"></i>
                  <span x-text="preset.plan === 'pro' ? 'Pro' : 'Free'"></span>
                </span>
              </div>
            </button>
          </template>
        </div>
        <div class="mt-6 rounded-2xl border border-dashed border-slate-300 bg-white/70 p-4 text-xs text-slate-500">
          <p class="font-semibold text-slate-600">Auto-sync options</p>
          <p class="mt-1">
            Use `/titles/api/export` to queue exports asynchronously and poll status at `/titles/api/export/status?id=`.
          </p>
        </div>
      </article>

      <aside class="space-y-6">
        <article class="rounded-3xl border border-slate-200 bg-white p-6 shadow-soft">
          <h3 class="text-lg font-semibold text-slate-900">Send to other Ansiversa workspaces</h3>
          <p class="mt-1 text-sm text-slate-600">
            Creative Title Maker integrates deeply with other creative and marketing mini apps.
          </p>
          <ul class="mt-4 space-y-3 text-sm text-slate-600">
            <template x-for="integration in maker.integrations" :key="integration.id">
              <li class="rounded-2xl border border-slate-200 bg-slate-50/80 p-4 shadow-sm">
                <div class="flex flex-wrap items-center justify-between gap-3">
                  <div>
                    <p class="font-semibold text-slate-900" x-text="integration.name"></p>
                    <p class="text-xs text-slate-500" x-text="integration.description"></p>
                  </div>
                  <span class="inline-flex h-10 w-10 items-center justify-center rounded-full bg-indigo-100 text-indigo-600">
                    <i class="fas" :class="integration.icon"></i>
                  </span>
                </div>
                <div class="mt-3 flex items-center justify-between text-xs text-slate-500">
                  <span class="font-semibold" x-text="integration.actionLabel"></span>
                  <Button href={integration.href} variant="ghost" size="sm" class="rounded-full !px-4">
                    <i class="fas fa-arrow-up-right-from-square text-[10px]"></i>
                    Open
                  </Button>
                </div>
              </li>
            </template>
          </ul>
        </article>

        <article class="rounded-3xl border border-indigo-100 bg-indigo-50 p-6 shadow-sm">
          <h3 class="text-lg font-semibold text-indigo-900">Automation recipe</h3>
          <ol class="mt-3 space-y-3 text-sm text-indigo-800">
            <li class="flex items-start gap-3">
              <span class="mt-0.5 flex h-6 w-6 items-center justify-center rounded-full bg-indigo-600 text-xs font-semibold text-white">1</span>
              <span>Call <code class="rounded bg-white/80 px-1">/titles/api/generate</code> with desired buckets and variants count.</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="mt-0.5 flex h-6 w-6 items-center justify-center rounded-full bg-indigo-600 text-xs font-semibold text-white">2</span>
              <span>Invoke <code class="rounded bg-white/80 px-1">/titles/api/score</code> for variants that pass your heuristics.</span>
            </li>
            <li class="flex items-start gap-3">
              <span class="mt-0.5 flex h-6 w-6 items-center justify-center rounded-full bg-indigo-600 text-xs font-semibold text-white">3</span>
              <span>Queue <code class="rounded bg-white/80 px-1">/titles/api/export</code> and fetch the ready file via `/titles/api/export/status`.</span>
            </li>
          </ol>
        </article>
      </aside>
    </div>
  </div>
</section>
