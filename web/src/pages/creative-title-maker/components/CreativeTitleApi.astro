---
import SectionHeading from '../../../components/SectionHeading.astro';
---
<section id="creative-title-apis" class="border-t border-slate-200 bg-slate-50 py-16" x-data="() => ({ maker: $store.creativeTitleMaker })">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <SectionHeading
      eyebrow="API surface"
      title="Automate generation, scoring, localization, and exports"
      description="Creative Title Maker ships with SSR-ready endpoints that mirror the workspace flow. Wire them into your pipelines, batch jobs, or webhook automations."
    />

    <div class="mt-10 grid gap-6 lg:grid-cols-2">
      <template x-for="endpoint in maker.apiEndpoints" :key="`${endpoint.method}-${endpoint.path}`">
        <article class="rounded-3xl border border-slate-200 bg-white p-6 shadow-soft">
          <div class="flex items-center justify-between">
            <span class="inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-semibold"
              :class="endpoint.method === 'POST' ? 'bg-indigo-100 text-indigo-600' : 'bg-slate-100 text-slate-600'"
            >
              <i class="fas fa-code text-[10px]"></i>
              <span x-text="endpoint.method"></span>
            </span>
            <code class="rounded bg-slate-100 px-2 py-1 text-xs text-slate-600" x-text="endpoint.path"></code>
          </div>
          <p class="mt-3 text-sm text-slate-600" x-text="endpoint.description"></p>
        </article>
      </template>
    </div>

    <div class="mt-8 rounded-3xl border border-dashed border-slate-300 bg-white/70 p-6 text-sm text-slate-600">
      <p class="font-semibold text-slate-800">Websocket option</p>
      <p class="mt-1">
        Subscribe to `/titles/ws` for live counters, autosave notifications, and collaborative status pings.
      </p>
      <p class="mt-3 text-xs text-slate-500">
        All endpoints return structured JSON aligned with the data model outlined above so you can hydrate your own UI quickly.
      </p>
    </div>
  </div>
</section>
