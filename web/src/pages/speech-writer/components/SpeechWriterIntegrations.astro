---
import { SectionHeading } from '@ansiversa/components';
---
<section class="bg-slate-950 py-20 text-white">
  <div class="mx-auto max-w-6xl space-y-10 px-4 sm:px-6 lg:px-8" x-data="() => ({ speech: Alpine.store('speechWriter') })">
    <SectionHeading
      eyebrow="Integrations"
      title="Works hand-in-hand with Ansiversa companions"
      description="Send bullets to Presentation Designer, craft standout titles, and loop in meeting notes without breaking flow."
    />
    <div class="grid gap-6 md:grid-cols-3">
      <template x-for="integration in speech.integrations" :key="integration.id">
        <article class="flex h-full flex-col gap-3 rounded-3xl border border-white/10 bg-white/5 p-6 text-left text-xs text-indigo-100">
          <div class="flex items-center justify-between gap-3">
            <div class="flex items-center gap-2 text-sm font-semibold text-white">
              <i :class="`${integration.icon} text-indigo-200`"></i>
              <span x-text="integration.name"></span>
            </div>
            <span class="rounded-full bg-indigo-500/20 px-3 py-1 text-[10px] font-semibold uppercase tracking-wide text-indigo-100" x-text="integration.status"></span>
          </div>
          <p x-text="integration.description"></p>
          <ul class="mt-2 space-y-2">
            <template x-for="action in integration.actions" :key="action">
              <li class="flex items-start gap-2">
                <i class="fas fa-arrow-trend-up text-[9px] text-emerald-300"></i>
                <span x-text="action"></span>
              </li>
            </template>
          </ul>
        </article>
      </template>
    </div>
    <div class="rounded-3xl border border-white/10 bg-white/5 p-6 text-left text-xs text-indigo-100">
      <div class="flex flex-wrap items-center justify-between gap-3">
        <div>
          <p class="text-sm font-semibold text-white">Automation roadmap</p>
          <p class="mt-1">Upcoming connectors accelerate alignment between speech writing and delivery.</p>
        </div>
        <span class="inline-flex items-center gap-2 rounded-full bg-indigo-500/20 px-3 py-1 text-[10px] font-semibold uppercase tracking-wide text-indigo-100">
          <i class="fas fa-compass text-[9px]"></i>
          In discovery
        </span>
      </div>
      <ul class="mt-3 space-y-2">
        <li class="flex items-start gap-2">
          <i class="fas fa-plug text-[9px] text-sky-300"></i>
          CRM sync to personalise stakeholder briefings with updated account intel.
        </li>
        <li class="flex items-start gap-2">
          <i class="fas fa-waveform-lines text-[9px] text-sky-300"></i>
          Live rehearsal timers broadcasting to companion mobile app for remote producers.
        </li>
        <li class="flex items-start gap-2">
          <i class="fas fa-language text-[9px] text-sky-300"></i>
          Instant translation handoff to Language Flashcards for bilingual practice decks.
        </li>
      </ul>
    </div>
  </div>
</section>
