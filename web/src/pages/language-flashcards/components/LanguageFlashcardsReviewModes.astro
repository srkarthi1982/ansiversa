<section class="bg-white py-16">
  <div class="mx-auto max-w-6xl space-y-10 px-4 sm:px-6 lg:px-8">
    <header class="text-center lg:text-left">
      <p class="inline-flex items-center gap-2 rounded-full bg-slate-900 px-4 py-1.5 text-sm font-semibold text-white">
        <i class="fas fa-sliders text-xs"></i>
        Multi-mode review engine
      </p>
      <h2 class="mt-4 text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">Pronounce, type, and cloze in one flow</h2>
      <p class="mt-3 text-base text-slate-600 sm:text-lg">
        Switch modes mid-session, support RTL scripts, and use keyboard hotkeys for SM-2 grading without leaving the player.
      </p>
    </header>
    <div class="grid gap-6 lg:grid-cols-3">
      <template x-for="mode in app.state.reviewModes" :key="mode.id">
        <article class="group relative overflow-hidden rounded-3xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-xl">
          <div class="absolute inset-x-0 top-0 h-1.5 bg-gradient-to-r" :class="mode.accent"></div>
          <div class="flex items-start justify-between">
            <div>
              <p class="inline-flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">
                <i class="fas" :class="mode.icon"></i>
                <span x-text="mode.label"></span>
              </p>
              <p class="mt-3 text-sm text-slate-600" x-text="mode.description"></p>
            </div>
            <button
              type="button"
              class="inline-flex items-center gap-2 rounded-full border px-3 py-1 text-xs font-semibold transition"
              :class="app.state.review.mode === mode.id ? 'border-indigo-500 bg-indigo-500/10 text-indigo-600' : 'border-slate-200 bg-slate-50 text-slate-500 hover:border-indigo-300 hover:text-indigo-500'"
              @click.prevent="app.setReviewMode(mode.id)"
            >
              <i class="fas fa-check text-[10px]"></i>
              Use mode
            </button>
          </div>
          <ul class="mt-5 space-y-2 text-xs text-slate-500">
            <template x-for="step in mode.instructions" :key="step">
              <li class="flex items-start gap-2 rounded-2xl bg-slate-50 px-3 py-2">
                <i class="fas fa-circle text-[6px] text-indigo-400"></i>
                <span x-text="step"></span>
              </li>
            </template>
          </ul>
          <footer class="mt-6 rounded-2xl border border-dashed border-slate-200 bg-slate-50 px-4 py-3 text-xs text-slate-500">
            <p>
              <span class="font-semibold text-slate-700">Best for:</span>
              <span x-text="mode.recommendedFor"></span>
            </p>
            <p class="mt-1" x-show="mode.id === 'typing'">Supports script keyboards (kana, devanagari, arabic) with transliteration hints.</p>
            <p class="mt-1" x-show="mode.id === 'speak'">Pronunciation similarity uses lightweight heuristic scoring in v1.</p>
          </footer>
        </article>
      </template>
    </div>
  </div>
</section>
