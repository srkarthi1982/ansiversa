<section class="bg-white py-16">
  <div class="mx-auto max-w-6xl space-y-10 px-4 sm:px-6 lg:px-8">
    <header class="text-center lg:text-left">
      <p class="inline-flex items-center gap-2 rounded-full bg-indigo-100 px-4 py-1.5 text-sm font-semibold text-indigo-700">
        <i class="fas fa-crown text-xs"></i>
        Plans and limits
      </p>
      <h2 class="mt-4 text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">Scale from starter decks to full pro toolkit</h2>
      <p class="mt-3 text-base text-slate-600 sm:text-lg">Free keeps 3 decks with 100 daily reviews. Pro unlocks unlimited decks, phrase generator, audio recording, and share links.</p>
    </header>
    <div class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm">
      <table class="min-w-full divide-y divide-slate-200 text-sm text-slate-600">
        <thead class="bg-slate-50 text-xs font-semibold uppercase tracking-[0.3em] text-slate-500">
          <tr>
            <th class="px-4 py-3 text-left">Feature</th>
            <th class="px-4 py-3 text-left">Free</th>
            <th class="px-4 py-3 text-left">Pro</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-slate-200">
          <template x-for="row in app.state.planRows" :key="row.feature">
            <tr :class="row.highlight === 'pro' ? 'bg-indigo-50/60' : ''">
              <td class="px-4 py-3 font-semibold text-slate-900" x-text="row.feature"></td>
              <td class="px-4 py-3" x-text="row.free"></td>
              <td class="px-4 py-3 font-semibold text-slate-900" x-text="row.pro"></td>
            </tr>
          </template>
        </tbody>
      </table>
    </div>
    <div class="grid gap-6 lg:grid-cols-2">
      <div class="rounded-3xl border border-slate-200 bg-slate-50 p-6">
        <h3 class="text-lg font-semibold text-slate-900">Daily goals</h3>
        <p class="mt-2 text-sm text-slate-600">Set target reviews (20–2000) and new card introductions (0–50). Streak auto-extends when targets met before local midnight.</p>
        <button
          type="button"
          class="mt-4 inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-4 py-2 text-xs font-semibold text-slate-600 transition hover:border-indigo-300 hover:text-indigo-500"
          @click.prevent="app.setPanel('overview')"
        >
          <i class="fas fa-bullseye text-[10px]"></i>
          Adjust in workspace
        </button>
      </div>
      <div class="rounded-3xl border border-slate-200 bg-slate-50 p-6">
        <h3 class="text-lg font-semibold text-slate-900">Plan preview</h3>
        <p class="mt-2 text-sm text-slate-600">Toggle plan tier to compare features instantly.</p>
        <div class="mt-3 flex gap-2">
          <button
            type="button"
            class="inline-flex flex-1 items-center justify-center gap-2 rounded-full border px-4 py-2 text-xs font-semibold transition"
            :class="app.state.planTier === 'free' ? 'border-indigo-500 bg-indigo-500/10 text-indigo-600' : 'border-slate-200 bg-white text-slate-600 hover:border-indigo-300 hover:text-indigo-500'"
            @click.prevent="app.setPlanTier('free')"
          >
            <i class="fas fa-seedling text-[10px]"></i>
            Free
          </button>
          <button
            type="button"
            class="inline-flex flex-1 items-center justify-center gap-2 rounded-full border px-4 py-2 text-xs font-semibold transition"
            :class="app.state.planTier === 'pro' ? 'border-emerald-500 bg-emerald-500/10 text-emerald-600' : 'border-slate-200 bg-white text-slate-600 hover:border-emerald-300 hover:text-emerald-600'"
            @click.prevent="app.setPlanTier('pro')"
          >
            <i class="fas fa-meteor text-[10px]"></i>
            Pro
          </button>
        </div>
        <p class="mt-3 rounded-2xl border border-dashed border-slate-300 bg-white px-4 py-3 text-xs text-slate-500">
          Plan tier selection affects auto-generator breadth and review caps. Pro unlocks shareable deck links.
        </p>
      </div>
    </div>
  </div>
</section>
