<section class="bg-slate-950 py-16 sm:py-20">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8" x-data="() => ({ scanner: Alpine.store('smartTextbookScanner') })">
    <header class="max-w-3xl">
      <p class="text-sm font-semibold uppercase tracking-wide text-indigo-300">Data model</p>
      <h2 class="mt-2 text-3xl font-bold text-white sm:text-4xl">Structured entities keep content traceable</h2>
      <p class="mt-3 text-sm text-indigo-100 sm:text-base">
        BookScan, SourceAsset, Page, Block, and Artifact tables power exports and integrations. Every artifact references its
        source blocks for auditability.
      </p>
    </header>
    <div class="mt-10 space-y-6">
      <template x-for="entity in scanner.dataModel" :key="entity.name">
        <div class="rounded-3xl border border-white/10 bg-white/5 p-6 text-indigo-100">
          <div class="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between">
            <div>
              <p class="text-lg font-semibold text-white" x-text="entity.name"></p>
              <p class="text-sm text-indigo-200" x-text="entity.description"></p>
            </div>
          </div>
          <div class="mt-4 overflow-x-auto">
            <table class="min-w-full divide-y divide-white/10 text-left text-xs text-indigo-100">
              <thead class="bg-white/5 text-[11px] uppercase tracking-wide text-indigo-200">
                <tr>
                  <th scope="col" class="px-4 py-2 font-semibold">Field</th>
                  <th scope="col" class="px-4 py-2 font-semibold">Type</th>
                  <th scope="col" class="px-4 py-2 font-semibold">Notes</th>
                </tr>
              </thead>
              <tbody class="divide-y divide-white/5">
                <template x-for="field in entity.fields" :key="field.name">
                  <tr class="bg-white/5 odd:bg-white/0">
                    <td class="px-4 py-2 text-sm font-semibold text-white" x-text="field.name"></td>
                    <td class="px-4 py-2 text-sm text-indigo-100" x-text="field.type"></td>
                    <td class="px-4 py-2 text-sm text-indigo-200" x-text="field.notes"></td>
                  </tr>
                </template>
              </tbody>
            </table>
          </div>
        </div>
      </template>
    </div>
  </div>
</section>
