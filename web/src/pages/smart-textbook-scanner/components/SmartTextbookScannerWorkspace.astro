---
import Button from '../../../components/Button.astro';
---
<section class="bg-slate-950 py-20 sm:py-24">
  <div
    class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8"
    x-data="() => ({ scanner: Alpine.store('smartTextbookScanner') })"
  >
    <header class="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between">
      <div>
        <p class="text-sm font-semibold uppercase tracking-wide text-indigo-300">Workspace</p>
        <h2 class="mt-2 text-3xl font-bold text-white sm:text-4xl">Review every block with anchors and confidence</h2>
        <p class="mt-3 max-w-2xl text-sm text-indigo-100 sm:text-base">
          Tabs organise the workflow: monitor pipeline health, page previews, semantic blocks, exercises, and generated
          artifacts. Toggle a block to flag it for human review — the guardrails keep citations and LaTeX in sync.
        </p>
      </div>
      <div class="rounded-2xl border border-indigo-400/40 bg-indigo-400/10 p-4 text-sm text-indigo-100">
        <p class="font-semibold text-white" x-text="scanner.activeTab?.label ?? 'Overview'"></p>
        <p class="mt-1 text-xs text-indigo-200" x-text="scanner.activeTab?.summary ?? 'Select a tab to drill into details.'"></p>
        <div class="mt-2 flex flex-wrap gap-2 text-[11px] text-indigo-200">
          <template x-for="insight in (scanner.activeTab?.insights ?? [])" :key="insight">
            <span class="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1">
              <i class="fas fa-sparkles text-[9px] text-emerald-300"></i>
              <span x-text="insight"></span>
            </span>
          </template>
        </div>
      </div>
    </header>
    <div class="mt-8 flex flex-wrap gap-3 text-sm font-semibold text-indigo-200">
      <template x-for="tab in scanner.workspaceTabs" :key="tab.id">
        <button
          type="button"
          class="inline-flex items-center gap-2 rounded-full border px-4 py-2 transition"
          :class="tab.id === scanner.activeWorkspaceTabId
            ? 'border-indigo-400 bg-indigo-400/20 text-white shadow-[0_12px_40px_rgba(79,70,229,0.35)]'
            : 'border-white/10 bg-white/5 hover:border-indigo-300/60 hover:bg-white/10'"
          @click="scanner.selectWorkspaceTab(tab.id)"
        >
          <i :class="['fas', tab.id === scanner.activeWorkspaceTabId ? 'fa-circle-dot' : 'fa-circle', 'text-[10px]']"></i>
          <span x-text="tab.label"></span>
        </button>
      </template>
    </div>
    <div class="mt-10 grid gap-8 lg:grid-cols-[0.9fr_1.1fr]">
      <aside class="space-y-6">
        <div class="rounded-3xl border border-white/10 bg-white/5 p-5 text-indigo-100">
          <p class="text-xs font-semibold uppercase tracking-wide text-indigo-300">Pages</p>
          <ul class="mt-4 space-y-3">
            <template x-for="page in scanner.pages" :key="page.id">
              <li>
                <button
                  type="button"
                  class="w-full rounded-2xl border p-4 text-left transition"
                  :class="page.id === scanner.activePageId
                    ? 'border-emerald-400/70 bg-emerald-400/15 text-emerald-100'
                    : 'border-white/10 bg-white/5 hover:border-indigo-300/60'"
                  @click="scanner.selectPage(page.id)"
                >
                  <div class="flex items-center justify-between gap-3">
                    <div>
                      <p class="text-sm font-semibold text-white">Page <span x-text="page.pageNumber"></span></p>
                      <p class="text-xs text-indigo-200" x-text="page.chapter"></p>
                    </div>
                    <span class="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1 text-[11px] font-semibold">
                      <i class="fas fa-bullseye text-[10px]"></i>
                      <span x-text="`${page.confidence}%`"></span>
                    </span>
                  </div>
                  <p class="mt-2 text-[11px] text-indigo-200" x-text="page.anchor"></p>
                  <p class="mt-1 text-[11px]" :class="page.hasCorrections ? 'text-amber-200' : 'text-indigo-200'">
                    <i :class="page.hasCorrections ? 'fas fa-flag' : 'fas fa-shield-check'"></i>
                    <span x-text="page.hasCorrections ? 'Corrections pending' : 'Verified' "></span>
                  </p>
                </button>
              </li>
            </template>
          </ul>
        </div>
        <div class="rounded-3xl border border-white/10 bg-white/5 p-5 text-xs text-indigo-100">
          <p class="font-semibold uppercase tracking-wide text-indigo-300">Quality guardrails</p>
          <p class="mt-2 text-indigo-200">Math LaTeX, citation anchors, and exercise pairs stay linked across exports.</p>
        </div>
      </aside>
      <div class="space-y-6">
        <div class="rounded-3xl border border-white/10 bg-white/5 p-6">
          <header class="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between">
            <div>
              <p class="text-xs font-semibold uppercase tracking-wide text-indigo-300">Blocks on selected page</p>
              <h3 class="text-xl font-semibold text-white" x-text="scanner.activePage?.chapter ?? 'Choose a page'"></h3>
            </div>
            <div class="flex flex-wrap items-center gap-2 text-[11px] text-indigo-200">
              <span class="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1">
                <i class="fas fa-diagram-project text-[10px]"></i>
                <span x-text="`${scanner.activePage?.blocks.length ?? 0} blocks`"></span>
              </span>
              <span class="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1">
                <i class="fas fa-anchor text-[10px]"></i>
                <span x-text="scanner.activePage?.anchor ?? '—'"></span>
              </span>
            </div>
          </header>
          <div class="mt-4 grid gap-4 lg:grid-cols-[0.65fr_0.35fr]">
            <div class="space-y-3">
              <template x-if="scanner.activePage">
                <template x-for="block in scanner.activePage.blocks" :key="block.id">
                  <article
                    class="rounded-2xl border p-4 text-sm transition"
                    :class="block.id === scanner.selectedBlockId
                      ? 'border-indigo-400/80 bg-indigo-400/15 text-indigo-50'
                      : block.flagged
                        ? 'border-amber-400/70 bg-amber-400/10 text-amber-100'
                        : 'border-white/10 bg-white/5 text-indigo-100'"
                  >
                    <div class="flex items-start justify-between gap-3">
                      <div>
                        <p class="text-sm font-semibold text-white" x-text="block.heading"></p>
                        <p class="mt-1 text-[11px] text-indigo-200" x-text="block.anchors.join(' · ')"></p>
                      </div>
                      <button
                        type="button"
                        class="inline-flex items-center gap-1 rounded-full border px-2 py-1 text-[10px] font-semibold uppercase tracking-wide"
                        :class="block.flagged ? 'border-amber-400/70 bg-amber-400/20 text-amber-100' : 'border-white/10 bg-white/5 text-indigo-100'"
                        @click="scanner.toggleBlockFlag(block.id)"
                      >
                        <i :class="block.flagged ? 'fas fa-flag' : 'fas fa-flag-checkered'"></i>
                        <span x-text="block.flagged ? 'Flagged' : 'Safe'"></span>
                      </button>
                    </div>
                    <p class="mt-2 text-sm" x-text="block.excerpt"></p>
                    <div class="mt-3 flex flex-wrap gap-2 text-[11px] text-indigo-200">
                      <span class="inline-flex items-center gap-1 rounded-full bg-white/10 px-2 py-0.5">
                        <i class="fas fa-shield-heart text-[9px]"></i>
                        <span x-text="`Confidence ${(block.confidence * 100).toFixed(0)}%`"></span>
                      </span>
                      <template x-for="badge in block.badges" :key="badge">
                        <span class="inline-flex items-center gap-1 rounded-full bg-white/10 px-2 py-0.5">
                          <i class="fas fa-tag text-[9px]"></i>
                          <span x-text="badge"></span>
                        </span>
                      </template>
                      <template x-if="block.pairedWith">
                        <span class="inline-flex items-center gap-1 rounded-full bg-emerald-400/20 px-2 py-0.5 text-emerald-100">
                          <i class="fas fa-link text-[9px]"></i>
                          <span x-text="`Paired → ${block.pairedWith}`"></span>
                        </span>
                      </template>
                    </div>
                    <div class="mt-3 flex flex-wrap items-center gap-3 text-[11px] text-indigo-200">
                      <Button type="button" size="sm" variant="ghost" class="text-indigo-200 hover:bg-white/10" @click.prevent="scanner.selectBlock(block.id)">
                        <i class="fas fa-eye text-[10px]"></i>
                        Focus
                      </Button>
                      <Button type="button" size="sm" variant="ghost" class="text-indigo-200 hover:bg-white/10" @click.prevent="scanner.generateArtifact('flashcards')">
                        <i class="fas fa-clone text-[10px]"></i>
                        Refresh cards
                      </Button>
                    </div>
                  </article>
                </template>
              </template>
              <template x-if="!scanner.activePage">
                <p class="rounded-2xl border border-dashed border-white/10 bg-white/5 p-6 text-center text-sm text-indigo-100">
                  Select a page to inspect its blocks.
                </p>
              </template>
            </div>
            <div class="rounded-2xl border border-white/10 bg-white/5 p-4 text-sm text-indigo-100">
              <template x-if="scanner.selectedBlock">
                <div>
                  <p class="text-xs font-semibold uppercase tracking-wide text-indigo-300">Block details</p>
                  <h4 class="mt-2 text-lg font-semibold text-white" x-text="scanner.selectedBlock.heading"></h4>
                  <p class="mt-2 text-sm" x-text="scanner.selectedBlock.excerpt"></p>
                  <p class="mt-3 text-[11px] text-indigo-200">Anchors</p>
                  <ul class="mt-1 space-y-1 text-[11px]">
                    <template x-for="anchor in scanner.selectedBlock.anchors" :key="anchor">
                      <li class="flex items-center gap-2">
                        <i class="fas fa-anchor text-[9px]"></i>
                        <span x-text="anchor"></span>
                      </li>
                    </template>
                  </ul>
                  <p class="mt-3 text-[11px] text-indigo-200">Confidence <span x-text="`${(scanner.selectedBlock.confidence * 100).toFixed(0)}%`"></span></p>
                  <p class="mt-2 text-[11px] text-indigo-200" x-text="scanner.selectedBlock.badges.join(' · ')"></p>
                </div>
              </template>
              <template x-if="!scanner.selectedBlock">
                <p class="text-sm text-indigo-200">Choose a block to view its details, anchors, and status.</p>
              </template>
            </div>
          </div>
        </div>
        <div class="rounded-3xl border border-indigo-400/40 bg-indigo-400/10 p-6 text-sm text-indigo-100">
          <p class="text-xs font-semibold uppercase tracking-wide text-indigo-300">Action log</p>
          <ul class="mt-3 space-y-2 text-[11px]">
            <template x-for="log in scanner.pipelineLog" :key="`workspace-${log.id}`">
              <li class="flex items-center justify-between gap-3 rounded-xl border border-white/10 bg-white/5 px-3 py-2">
                <span x-text="log.message"></span>
                <span class="text-[10px] font-semibold uppercase tracking-wide text-indigo-300" x-text="log.timestamp"></span>
              </li>
            </template>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>
