<section class="bg-slate-950 py-16 sm:py-20">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8" x-data="() => ({ scanner: Alpine.store('smartTextbookScanner') })">
    <div class="lg:flex lg:items-end lg:justify-between">
      <div>
        <p class="text-sm font-semibold uppercase tracking-wide text-indigo-300">Integrations</p>
        <h2 class="mt-2 text-3xl font-bold text-white sm:text-4xl">Push cleaned content into the Ansiversa ecosystem</h2>
        <p class="mt-3 max-w-2xl text-sm text-indigo-100 sm:text-base">
          Flashcards flow to FlashNote, checkpoints sync to Study Planner, and exercises power Homework Helper. All exports carry
          anchors so learners jump straight to the source page.
        </p>
      </div>
      <p class="mt-4 max-w-sm rounded-2xl border border-indigo-400/40 bg-indigo-400/10 p-4 text-sm text-indigo-100 lg:mt-0">
        <i class="fas fa-plug mr-2 text-indigo-300"></i>
        OAuth-secured integrations keep privacy intact — nothing publishes without your approval.
      </p>
    </div>
    <div class="mt-10 grid gap-6 sm:grid-cols-2">
      <template x-for="integration in scanner.integrations" :key="integration.id">
        <article class="rounded-3xl border border-white/10 bg-white/5 p-6 text-indigo-100">
          <div class="flex items-start justify-between gap-3">
            <div class="flex items-center gap-3">
              <span class="inline-flex h-10 w-10 items-center justify-center rounded-full bg-indigo-500/20 text-indigo-200">
                <i :class="integration.icon"></i>
              </span>
              <div>
                <p class="text-lg font-semibold text-white" x-text="integration.name"></p>
                <p class="text-xs text-indigo-200" x-text="integration.description"></p>
              </div>
            </div>
            <span
              class="inline-flex items-center gap-2 rounded-full px-3 py-1 text-[11px] font-semibold uppercase tracking-wide"
              :class="integration.status === 'push'
                ? 'bg-emerald-400/20 text-emerald-100'
                : integration.status === 'sync'
                  ? 'bg-indigo-400/20 text-indigo-100'
                  : 'bg-white/10 text-indigo-100'"
            >
              <i
                :class="[
                  integration.status === 'push' ? 'fas fa-arrow-right-arrow-left' : integration.status === 'sync' ? 'fas fa-sync' : 'fas fa-eye',
                  'text-[10px]'
                ]"
              ></i>
              <span x-text="integration.status === 'push' ? 'Two-way' : integration.status === 'sync' ? 'Sync' : 'View only'"></span>
            </span>
          </div>
          <p class="mt-4 rounded-2xl border border-white/10 bg-white/5 p-3 text-sm" x-text="integration.highlight"></p>
        </article>
      </template>
    </div>
  </div>
</section>
