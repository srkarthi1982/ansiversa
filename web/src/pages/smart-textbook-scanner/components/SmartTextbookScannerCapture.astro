---
import { Button } from '@ansiversa/components';
---
<section class="bg-slate-950 py-16 sm:py-20">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8" x-data="() => ({ scanner: Alpine.store('smartTextbookScanner') })">
    <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
      <div>
        <p class="text-sm font-semibold uppercase tracking-wide text-indigo-300">Capture and ingest</p>
        <h2 class="mt-2 text-3xl font-bold text-white sm:text-4xl">Choose how you bring textbooks into the scanner</h2>
        <p class="mt-3 max-w-2xl text-sm text-indigo-100 sm:text-base">
          Every capture path enforces DPI checks, glare detection, and page order integrity. Pick one or chain multiple sources;
          the pipeline dedupes and merges them before preprocessing.
        </p>
      </div>
      <Button type="button" variant="outline" class="border-indigo-400/60 text-indigo-200" @click.prevent="scanner.startCapture()">
        <i class="fas fa-play text-xs"></i>
        Begin capture now
      </Button>
    </div>
    <div class="mt-10 grid gap-6 lg:grid-cols-3">
      <template x-for="mode in scanner.captureModes" :key="mode.id">
        <article
          class="relative flex h-full flex-col rounded-3xl border bg-slate-900/60 p-6 text-indigo-100 transition hover:border-indigo-400/60 hover:bg-slate-900"
          :class="mode.id === scanner.activeCaptureModeId ? 'border-indigo-400/80 shadow-[0_20px_60px_rgba(79,70,229,0.35)]' : 'border-white/10'"
        >
          <div class="flex items-start justify-between gap-3">
            <div class="flex items-center gap-3">
              <span class="inline-flex h-10 w-10 items-center justify-center rounded-full bg-indigo-500/20 text-indigo-200">
                <i :class="mode.icon"></i>
              </span>
              <div>
                <h3 class="text-lg font-semibold text-white" x-text="mode.label"></h3>
                <p class="text-xs text-indigo-200" x-text="mode.limit"></p>
              </div>
            </div>
            <button
              type="button"
              class="inline-flex items-center gap-2 rounded-full border px-3 py-1 text-[11px] font-semibold uppercase tracking-wide transition"
              :class="mode.id === scanner.activeCaptureModeId ? 'border-emerald-400/80 bg-emerald-400/20 text-emerald-100' : 'border-white/10 bg-white/5 text-indigo-100 hover:border-indigo-300/80'"
              @click="scanner.selectCaptureMode(mode.id)"
            >
              <i class="fas fa-circle-dot text-[10px]"></i>
              <span x-text="mode.id === scanner.activeCaptureModeId ? 'Selected' : 'Select'"></span>
            </button>
          </div>
          <p class="mt-4 text-sm" x-text="mode.description"></p>
          <p class="mt-4 text-xs font-semibold uppercase tracking-wide text-indigo-300">Automation boosts</p>
          <ul class="mt-2 space-y-2 text-sm">
            <template x-for="auto in mode.automation" :key="auto">
              <li class="flex items-start gap-2">
                <i class="fas fa-wand-magic-sparkles mt-0.5 text-[10px] text-emerald-300"></i>
                <span x-text="auto"></span>
              </li>
            </template>
          </ul>
          <p class="mt-4 rounded-2xl border border-indigo-400/30 bg-indigo-400/10 p-3 text-xs text-indigo-100" x-text="mode.highlight"></p>
        </article>
      </template>
    </div>
  </div>
</section>
