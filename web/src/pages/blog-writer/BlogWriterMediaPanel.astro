---
import Button from "../../components/Button.astro";
---
<section class="space-y-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
  <header class="flex flex-col gap-2">
    <div class="flex items-center justify-between gap-2">
      <h2 class="text-lg font-semibold text-slate-900">Media and prompts</h2>
      <Button type="button" size="sm" variant="outline" @click.prevent="$store['blog-writer'].generateImagePrompts()">
        <i class="fas fa-image text-xs"></i>
        AI prompts
      </Button>
    </div>
    <p class="text-sm text-slate-600">
      Attach hero and inline images. Use the prompt helper to ideate visuals that reinforce your story.
    </p>
  </header>
  <div class="space-y-4">
    <div class="space-y-2">
      <p class="text-xs font-semibold uppercase tracking-wide text-slate-500">Library</p>
      <template x-if="$store['blog-writer'].builder.record.media.length === 0">
        <p class="rounded-2xl border border-dashed border-slate-200 bg-slate-50 p-4 text-sm text-slate-500">
          No media yet. Upload images in the next iteration or convert prompts into assets.
        </p>
      </template>
      <ul class="space-y-3">
        <template x-for="media in $store['blog-writer'].builder.record.media" :key="media.id">
          <li class="flex items-start gap-3 rounded-2xl border border-slate-100 bg-white p-3 shadow-sm">
            <img :src="media.url" :alt="media.alt" class="h-12 w-12 rounded-xl object-cover" />
            <div class="flex-1 space-y-1 text-xs text-slate-600">
              <p class="font-semibold text-slate-900" x-text="media.filename"></p>
              <p x-text="media.caption || media.alt"></p>
              <p class="text-[11px] text-slate-400">
                <span x-text="Math.round(media.sizeKb)"></span> KB • added <span x-text="$store['blog-writer'].formatRelative(media.createdAt)"></span>
              </p>
            </div>
            <button
              type="button"
              class="inline-flex h-8 w-8 items-center justify-center rounded-full text-rose-500 hover:bg-rose-50"
              @click.prevent="$store['blog-writer'].removeMedia(media.id)"
            >
              <i class="fas fa-times text-xs"></i>
            </button>
          </li>
        </template>
      </ul>
    </div>
    <div class="space-y-3">
      <div class="flex items-center justify-between">
        <p class="text-xs font-semibold uppercase tracking-wide text-slate-500">Prompt ideas</p>
        <span class="text-[11px] text-slate-500" x-show="$store['blog-writer'].builder.imageLoading">Generating…</span>
      </div>
      <template x-if="$store['blog-writer'].builder.imagePrompts.length === 0">
        <p class="rounded-2xl border border-dashed border-slate-200 bg-slate-50 p-4 text-sm text-slate-500">
          Use “AI prompts” above to fetch concepts for hero, inline, and diagram visuals.
        </p>
      </template>
      <div class="space-y-2">
        <template x-for="prompt in $store['blog-writer'].builder.imagePrompts" :key="prompt.id">
          <div class="rounded-2xl border border-slate-200 bg-white p-3 text-xs text-slate-600 shadow-sm">
            <div class="flex items-start justify-between gap-2">
              <div class="space-y-1">
                <p class="font-semibold text-slate-900 capitalize" x-text="prompt.aspect"></p>
                <p x-text="prompt.description"></p>
              </div>
              <Button type="button" size="sm" variant="light" @click.prevent="$store['blog-writer'].addMediaFromPrompt(prompt)">
                <i class="fas fa-download text-xs"></i>
                Save
              </Button>
            </div>
          </div>
        </template>
      </div>
    </div>
  </div>
</section>
