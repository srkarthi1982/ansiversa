---
import Layout from '../../../layouts/Layout.astro';
---
<Layout>
  <main class="min-h-screen bg-slate-50">
    <section class="py-16" x-data="() => ({ app: $store.memoryTrainer })">
      <div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8">
        <h1 class="text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">
          Memory Trainer Admin
        </h1>
        <p class="mt-3 text-sm text-slate-600">
          Streaks, reminders, and export pipelines share infrastructure with FlashNote and Quiz Institute. Adjust service hooks
          here before rolling out to production tenants.
        </p>
        <div class="mt-8 grid gap-5 sm:grid-cols-2">
          <div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
            <p class="text-xs font-semibold uppercase tracking-wide text-indigo-500">Streak snapshot</p>
            <p class="mt-2 text-3xl font-bold text-slate-900" x-text="`${app.state.streak.current} days`"></p>
            <p class="text-xs text-slate-500">Best streak <span x-text="app.state.streak.best"></span></p>
          </div>
          <div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
            <p class="text-xs font-semibold uppercase tracking-wide text-indigo-500">Reminder status</p>
            <p class="mt-2 text-3xl font-bold text-slate-900" x-text="app.state.reminder.enabled ? 'Active' : 'Paused'"></p>
            <p class="text-xs text-slate-500">Time <span x-text="app.state.reminder.time"></span> · Days <span x-text="app.state.reminder.days.join(', ')"></span></p>
          </div>
        </div>
      </div>
    </section>
  </main>
</Layout>
