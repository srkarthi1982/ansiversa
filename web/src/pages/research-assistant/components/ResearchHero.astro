---
import { Button } from '@ansiversa/components';
import { heroHighlights } from "../data/researchData";
---
<section class="relative overflow-hidden bg-slate-950 py-20 text-white" aria-labelledby="research-hero-title">
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(79,70,229,0.35),_transparent_60%)]"></div>
  <div class="relative mx-auto flex max-w-6xl flex-col gap-16 px-4 sm:px-6 lg:px-8">
    <div class="grid gap-12 lg:grid-cols-[1.1fr_0.9fr] lg:items-center">
      <div class="space-y-8">
        <span class="inline-flex items-center gap-2 rounded-full bg-indigo-500/20 px-4 py-1 text-sm font-semibold text-indigo-200">
          <i class="fas fa-lightbulb-on text-xs"></i>
          Evidence engine for serious research
        </span>
        <div class="space-y-4">
          <h1 id="research-hero-title" class="text-4xl font-bold tracking-tight sm:text-5xl">
            Research Assistant
          </h1>
          <p class="text-base text-slate-200 sm:text-lg">
            Plan rigorous projects, capture annotated highlights, and synthesize claims into publication-ready drafts with
            citations you can trust. Reader, synthesis, writer, and export tools work in one orchestrated workspace.
          </p>
        </div>
        <div class="flex flex-wrap gap-3">
          <Button href="#research-workspaces" class="shadow-lg shadow-indigo-500/30">
            <i class="fas fa-diagram-project text-xs"></i>
            Explore workspace
          </Button>
          <Button href="#research-user-stories" variant="outline" class="border-indigo-300 text-indigo-200 hover:bg-indigo-500/10">
            <i class="fas fa-list-check text-xs"></i>
            Review requirements
          </Button>
          <Button href="#research-integrity" variant="ghost" class="text-slate-200 hover:bg-white/10">
            <i class="fas fa-shield-check text-xs"></i>
            See trust guardrails
          </Button>
        </div>
        <dl class="grid gap-4 sm:grid-cols-3">
          <div class="rounded-2xl border border-white/20 bg-white/5 p-4 shadow-lg backdrop-blur">
            <dt class="text-xs font-semibold uppercase tracking-wide text-indigo-200">Projects supported</dt>
            <dd class="mt-2 text-3xl font-semibold">Academic · Product · Policy</dd>
          </div>
          <div class="rounded-2xl border border-white/20 bg-white/5 p-4 shadow-lg backdrop-blur">
            <dt class="text-xs font-semibold uppercase tracking-wide text-indigo-200">Evidence traces</dt>
            <dd class="mt-2 text-3xl font-semibold">Highlights → Claims → Draft</dd>
          </div>
          <div class="rounded-2xl border border-white/20 bg-white/5 p-4 shadow-lg backdrop-blur">
            <dt class="text-xs font-semibold uppercase tracking-wide text-indigo-200">Citations</dt>
            <dd class="mt-2 text-3xl font-semibold">APA · MLA · Chicago · IEEE</dd>
          </div>
        </dl>
      </div>
      <div class="relative">
        <div class="absolute inset-0 -translate-y-6 translate-x-6 rounded-3xl bg-indigo-500/40 blur-3xl"></div>
        <div class="relative space-y-5 rounded-3xl border border-white/10 bg-slate-900/60 p-6 shadow-2xl backdrop-blur">
          <h2 class="text-lg font-semibold text-white">Workflow snapshot</h2>
          <ol class="space-y-4 text-sm text-slate-200">
            <li class="flex items-start gap-3">
              <span class="mt-1 flex h-7 w-7 items-center justify-center rounded-full bg-indigo-500 text-xs font-semibold text-white">1</span>
              <div>
                <p class="font-semibold">Create a project</p>
                <p class="text-xs text-slate-300">Define question, scope, and citation style with plan-aware gating.</p>
              </div>
            </li>
            <li class="flex items-start gap-3">
              <span class="mt-1 flex h-7 w-7 items-center justify-center rounded-full bg-indigo-500 text-xs font-semibold text-white">2</span>
              <div>
                <p class="font-semibold">Collect and read sources</p>
                <p class="text-xs text-slate-300">Fetch or upload references, highlight passages, and capture linked notes.</p>
              </div>
            </li>
            <li class="flex items-start gap-3">
              <span class="mt-1 flex h-7 w-7 items-center justify-center rounded-full bg-indigo-500 text-xs font-semibold text-white">3</span>
              <div>
                <p class="font-semibold">Synthesize and draft</p>
                <p class="text-xs text-slate-300">Build claim matrices, surface counterpoints, and export cited drafts.</p>
              </div>
            </li>
          </ol>
          <div class="rounded-2xl border border-white/10 bg-white/5 p-4 text-xs text-slate-100">
            <p class="font-semibold uppercase tracking-wide text-indigo-200">Highlights</p>
            <div class="mt-3 grid gap-3">
              {heroHighlights.map((item) => (
                <div class="flex items-start gap-3" key={item.label}>
                  <span class="mt-0.5 rounded-full bg-indigo-500/20 p-2 text-indigo-200">
                    <i class={`${item.icon} text-[11px]`}></i>
                  </span>
                  <div>
                    <p class="text-sm font-semibold text-white">{item.label}</p>
                    <p class="text-[11px] text-slate-300">{item.blurb}</p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
