---
import SectionHeading from '../../../components/SectionHeading.astro';
---
<section id="ai-meme-api" class="border-t border-slate-200 bg-slate-50 py-16" x-data="() => ({ meme: $store.aiMemeCreator })">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <SectionHeading
      eyebrow="API surface"
      title="Automate projects, captions, safety checks, and exports"
      description="Endpoints mirror the workspace flow so you can embed the Meme Creator into your own dashboards, bots, or creative pipelines."
    />

    <div class="mt-10 grid gap-6 lg:grid-cols-2">
      <template x-for="endpoint in meme.state.apiEndpoints" :key="`${endpoint.method}-${endpoint.path}`">
        <article class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
          <div class="flex items-center justify-between">
            <span
              class="inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-semibold"
              :class="endpoint.method === 'POST' ? 'bg-indigo-100 text-indigo-600' : 'bg-slate-100 text-slate-600'"
            >
              <i class="fas fa-code text-[10px]"></i>
              <span x-text="endpoint.method"></span>
            </span>
            <code class="rounded bg-slate-100 px-2 py-1 text-xs text-slate-600" x-text="endpoint.path"></code>
          </div>
          <p class="mt-3 text-sm text-slate-600" x-text="endpoint.description"></p>
        </article>
      </template>
    </div>

    <div class="mt-8 rounded-3xl border border-dashed border-slate-300 bg-white/80 p-6 text-sm text-slate-600">
      <p class="font-semibold text-slate-800">WebSocket and events</p>
      <p class="mt-1">
        Subscribe to `/meme/ws` for render progress, live tone score updates, and animation preview sync. Export webhooks include audit metadata and brand preset IDs.
      </p>
      <p class="mt-3 text-xs text-slate-500">All endpoints return structured JSON aligned with MemeProject, Layer, Caption, and ExportJob entities.</p>
    </div>
  </div>
</section>
